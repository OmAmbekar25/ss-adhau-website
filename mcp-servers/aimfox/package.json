{
  "name": "aimfox-mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing the Aimfox LinkedIn outreach API (campaigns, leads, conversations, labels, templates, blacklist, webhooks) as tools.",
  "type": "module",
  "bin": {
    "aimfox-mcp-server": "src/index.js"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  },
  "license": "MIT"
}
