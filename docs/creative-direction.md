# Creative Direction

**Location:** `docs/creative-direction.md`
**Referenced from:** `CLAUDE.md` — *Read this file before building or restyling any UI. It is the source of truth for all visual decisions.*
**Status:** live — sections previously marked `TO DECIDE` have been filled with the decisions already made and approved during the v3 build. Remaining open items are marked.

---

## How to use this file

This is not a mood board. It is a constraint document. Every section is either:

- **LOCKED** — a decision has been made. Follow it exactly. Do not "improve" it.
- **TO DECIDE** — not yet decided. Do not invent a value here and proceed. Stop and ask.

If a request conflicts with a LOCKED item, say so rather than silently resolving it.

---

## 0. Subject — `LOCKED`

| Field | Value |
| --- | --- |
| Company / product name | S S Adhau Valuers & Engineers |
| What it actually does, in one plain sentence | Government-registered valuers and chartered engineers who produce valuation reports for property, plant & machinery, and businesses. |
| Who the page is for (specific) | Bank branch/credit managers, chartered accountants, insolvency professionals (CIRP/liquidation), advocates needing court-admissible reports, property owners with tax valuations. |
| The single job this page has to do | Make the visitor believe a report signed by this firm will be accepted by a bank, court, or regulator without question. |
| What the visitor should be able to do 5 seconds after landing | Request a valuation (one obvious CTA). |
| Nearest competitor whose site we must not resemble | Any template-grade local valuer/CA site — and equally, any generic AI-startup dark-gradient site. |
| One true, specific, non-obvious thing about how the firm works | Every valuation starts with a physical site inspection by a registered valuer; every report is physically stamped and signed. |
| Proof we can show | IBBI Category I registration, Income Tax Dept. approval, 15 named public-sector banks/institutions that accept the firm's reports, two real offices, nine served cities. |

**The buyer is buying credibility and compliance, not creativity.** Target feel: a serious professional firm that happens to have a beautifully built site — audit firm / barristers' chambers, not agency.

---

## 1. The bar

**LOCKED — product-truth premium, with at most two spectacle moments per page,
and never two competing for the same screen.** *(Amended 2026-07-29 — was
"exactly one per page"; see §15.)*

- Homepage spectacle 1: the hero — the photographic shattered-glass plate with cursor-driven light (user-supplied v3 design, ported to React, no WebGL).
- Homepage spectacle 2: the valuation journey — the 3D narrative spine (`ValuationJourney` + `lib/reportScene.js`). It sits five screens below the hero, so the two never share a viewport.
- Locations page spectacle: the 3D map journey (survey beacon travelling the real MP/MH geography).
- Everything else is quiet, disciplined, near-invisible.

**The second spectacle has to earn it by being product-truth.** The spine is
not decoration: it is the firm's own five-step process, told as one field of
points that is re-formed — enquiry → surveyed site → ordered analysis →
stamped page → delivered originals. If a future addition cannot make that
claim, it does not get to be spectacle number two.

---

## 2. Locked constraints

### 2.1 Spend boldness in one place
**LOCKED.** One signature element per page (see §1). Nothing else competes with it.

### 2.2 Animation library — one system, not four
**LOCKED (and matches the current build):**
- **Lenis** — smooth scroll, site-wide (`SmoothScroll.jsx`).
- **GSAP + ScrollTrigger** — all scroll choreography (map journey pins, scrubs).
- **Motion** (`motion/react`) — component-level entrances and hover only.
- **Three.js** — two places only: the Locations map journey, and the homepage
  valuation journey (`lib/reportScene.js`). Desktop only (≥768px), lazy, never
  on first paint, capability-gated (a failed WebGL context falls back to the
  static step list). **The homepage hero stays deliberately CSS-only** — the
  2026-07-27 decision against a WebGL hero is untouched and still LOCKED.
  *(Amended 2026-07-29 — was "only inside the Locations map journey"; see §15.)*

### 2.3 Performance budget
**LOCKED.** As specified: LCP < 2.0s (4G, mid-tier Android), CLS < 0.05, INP < 200ms, first-load JS ≤ 250KB gz (excl. 3D chunk), 3D lazy + capability-gated, **`transform`/`opacity` only for anything driven per-frame by scroll; bounded paint properties (stroke-dash on small SVGs, ≤ once per trigger) permitted for entrance and hover**, fonts self-hosted via `next/font` (max 3 weights per family), `next/image` everywhere.
*(Amended 2026-08-01 — was "animate `transform`/`opacity` only"; the rule was
written against per-frame cost and read as a ban on a property. See §15.)*
Known debt against this budget (measured 2026-07-29, `next build`, gzipped
first-load chunk set per prerendered document):

| Page | First-load JS (gz) | Contains three.js? |
| --- | --- | --- |
| `/` | **288KB** (was 285KB before the valuation journey) | no — deferred |
| `/studio` | **225KB** ✓ under budget | no — deferred |
| `/about` | 224KB | no |
| `/locations` | 401KB | **yes — pre-existing** |

- The homepage was already over the 250KB budget before the valuation journey;
  the journey adds ~3KB because `reportScene.js` is a dynamic `import()` fired
  from inside the section's effect, so all ~139KB gz of three.js loads only
  when a desktop visitor reaches that section. **Open:** the ~38KB overage is
  inherited, not introduced — chase it in `motion` / `lucide-react` /
  `gsap` import surface, not in the 3D.
- `/locations` ships three.js in first-load JS because `MapJourney` imports
  `lib/map3d` statically. **Open** — should use the same dynamic-import
  pattern as `ValuationJourney`.
- `/studio` is the first page in the project to meet this budget, despite
  being the most visually ambitious: its three.js chunk is behind a dynamic
  `import()` fired from `requestIdleCallback`, and it carries no photography.
- The hero's flare/bloom layers use `mask-position`/`filter` compositing
  (accepted — static textures, GPU-composited, measured smooth); mobile map
  journey serves a static SVG instead of WebGL ✓.

### 2.4 Quality floor
**LOCKED.** `prefers-reduced-motion` = complete static site, not degraded (already implemented per component). Keyboard focus everywhere. Semantic HTML, one H1 per page. Contrast 4.5:1 body / 3:1 display — including over the glass photograph (the veil layer exists for this; check it whenever hero copy changes). No scroll hijacking. Touch targets ≥ 44px.

### 2.5 Copy is a design deliverable
**LOCKED.** Current approved copy set: hero H1 "Every Decision Begins With The Right Value."; supporting Central-India paragraph; CTAs "Request a Valuation" / "Explore Our Services". Claims carry credentials (IBBI · Category I, Income Tax Dept.) or names (the 15 institutions). No "X, redefined" constructions.

---

## 3. Brand personality — `LOCKED`

| Adjective | Therefore we never... |
| --- | --- |
| Authoritative | use playful motion, bouncy easing, cartoon iconography, or emoji |
| Precise | ship placeholder data (Est. XXXX), invent case studies, or use approximate credentials |
| Understated-luxurious | let gold cover large surfaces — brass is an accent caught in edges, hairlines, and type, never a fill for panels |

Voice as a person: a senior chartered engineer who has signed thirty years of reports — calm, exact, unhurried.
Adjacent brand in an unrelated category: a luxury watchmaker's product catalogue (Omega reference in `hero-reference/10`).

---

## 4. Typography system — `LOCKED`

```
Display:  Cormorant Garamond — weights 300/400/500 + italic — all h1/h2/h3
          site-wide (set in globals.css). Justification: calligraphic serif
          reads as engraved/certificate — the firm's product is a signed,
          sealed document. Not on the AI-default list.
Body:     Archivo (hero) / IBM Plex Sans (site body) — 400/500(/600 Plex).
Utility:  IBM Plex Mono — eyebrows, buttons, credential rows, nav-adjacent
          labels. Carries the "technical document" register.

Hero H1: clamp(60px, 11vw, 168px) equivalent (see Hero.jsx), lh 0.94–0.98.
The metallic headline gradient (#EED7A1→#C79A43→#A87325 family) is applied
to the focal line only.
```

Self-hosted via `next/font` (Google + local). No Inter, no Satoshi (removed).

**The voice rule, amended 2026-08-01 (the block above is superseded by the
2026-07-31 and 2026-08-01 §15 rows; this is the operative statement):**
tracked uppercase is the LABEL voice — four words or fewer, naming a thing.
**Any full sentence is reading text, whatever position it sits in — including a
closing line, a strapline or a sign-off.** Position does not make a sentence a
label. Reading text takes the reading face. See §15 for the ruling.

---

## 5. Color system — `LOCKED`

```
--noir      #070606   the stage. True near-black, from the v3 hero plate.
--noir-2    #100E0C   raised surfaces (rarely needed; prefer white/[0.04]).
--linen     #F2EFE9   headings + primary text. Warm white from the plate's
                      highlights.
--fog       #9C9A93   body/muted text.
--brass     #C9A063   THE accent. Used ONLY for: eyebrows, hairline rules,
                      edge accents, CTA borders/fills, credential markers.
--hairline  rgba(242,239,233,0.12)  borders and dividers.
```

Every value derives from the hero photograph — the site is lit by the same
light as its signature image. Panels are `bg-white/[0.04]` + hairline
border, never solid gray. **No blue anywhere** (the old navy identity was
retired deliberately; do not reintroduce it in UI — it survives only inside
untouchable assets like bank logos and the light paper map).

---

## 6. Spacing & grid — `LOCKED`
As specified in the scaffold (8px base, 12/6/4 grid, 1440px max, 60–75ch reading column, section rhythm 2–3× internal spacing).

## 7. Motion principles — `LOCKED`
As specified in the scaffold. Two easings only: `cubic-bezier(0.16,1,0.3,1)` entrances, `cubic-bezier(0.2,0.8,0.2,1)` hovers (the v3 button curve). Reveals fire once. Max two pinned sections per page — the homepage is now **at that ceiling** (Spotlight's `Sticky`, and the valuation journey). Nothing else on the homepage may pin.

**Scroll-driven 3D is a pure function of progress.** `reportScene.js` derives
every value — morph, opacity, camera, the seal landing — from `p` alone. No
tweens, no one-shot state, so scrubbing backwards un-signs the report exactly.
Any future scroll scene follows this rule: if a moment can't be expressed as
`f(p)`, it doesn't belong in a scrubbed section.

**The property test is per-frame cost, not the property's name.** Anything a
scroll drives every frame is `transform`/`opacity` — the scope ledger's rules
scale, they do not redraw. Entrance and hover may use a bounded paint property
where no transform expresses the idea: stroke-dash on a small SVG, running at
most once per trigger. A drawing that draws itself cannot be a transform, and
30 paths inside nine 24px boxes are not a frame budget.
*(Codified 2026-08-01 with §2.3. See §15.)*

**Interaction moves the light, never the object.** The hero established it; the
valuation journey (pointer nudges the camera, not the geometry) and
`LightCard` (pointer moves a brass edge-glow, not the panel) inherit it.

**Known conflicts with the current build (flagged, not silently resolved — see §15):**
- The "Trusted by" section is an infinite logo lane (rejection list §14) — kept deliberately: user-directed, modelled on the AEOS reference, with center-spotlight behavior that goes beyond a plain marquee. Revisit if conversion suffers.
- The map journey finale counts 0→9 on arrival (motion §7 rejects scroll counters) — kept: it fires once at a narrative destination, not ambiently on scroll.

## 8. Scroll journey — `LOCKED` (homepage)
The argument: (1) hero — a report that holds up = the right value; (2) trusted-by — institutions already rely on it; (3) services — what we can value; (4) spotlight — how we actually work (site inspection, photographed); (5) **the valuation journey — the same process, in 3D, end to end**; (6) about/presence — depth for the diligent reader; (7) contact — the step. Candidates for the cut test: MarqueeCards testimonials and HomeContact overlap with Footer — review before launch.

Spotlight is deliberately placed immediately before the journey and labelled
"Step 02", which is the journey's second beat: the photograph and the 3D beat
are the same claim, told twice, in the order a reader meets them.

**The survey thread** (`SurveyThread.jsx`) is the connective tissue: one brass
hairline down the left gutter with a diamond beacon travelling it and a station
tick per chapter — the same beacon-on-a-route language as the Locations map
journey, met on the homepage first. Chapters come from `[data-chapter]`
attributes in `app/page.js`, so the rail cannot drift out of sync with the
sections. Desktop ≥1024px, fades in only after the hero, absent under reduced
motion.

## 9. Hero — `LOCKED`
The v3 shattered-glass plate with cursor-driven light. H1 in DOM before JS. One primary CTA. See `hero-reference/README.md` for the full art-direction bible and acceptance criteria.

## 10. Imagery & assets — partially `TO DECIDE`
Have: hero glass plate (user-supplied), valuelady.jpg (genuine editorial-grade), institution logos, real map data. **Rejected as cliché and pending replacement: consulting.jpg and law.jpg (scrabble-tile stock).** Needed from the firm: real office/team/site-inspection photography, real case-study data (`src/data/caseStudies.js` is intentionally empty), real Formspree endpoint confirmation, real production domain (.co.in vs placeholder).

## 11. Navigation, loading, footer, CTA — `LOCKED`
As specified. Current nav: 5 items + CTA ✓. No preloader ✓. CTA wording: "Request a Valuation" — use identically everywhere it appears.

## 12. Mobile — `LOCKED`
As specified. Map journey and hero already ship distinct mobile compositions.

## 13. Review rubric — `LOCKED`
Apply before showing any new section. The substitution test is the big one: navy-corporate templates fail it; the current noir/brass/serif language passes only as long as content stays specific to this firm.

## 14. Rejection list — `LOCKED`
As specified in the scaffold, plus (project-specific): no blue accents, no crystal/orbit-ring 3D objects (rejected in hero v2), no fabricated client work or placeholder credentials, no Satoshi/Inter.

---

## 15. Decisions log

| Date | Decision | Reason | Alternative rejected |
| --- | --- | --- | --- |
| 2026-07-26 | Brand register: dark luxury ("audit firm with a beautiful site") | User brief: buyers purchase credibility; AEOS-style creative-studio register rejected | Light paper + navy/gold institutional palette (v1) |
| 2026-07-27 | Hero = user-supplied v3 glass plate, CSS-only; three.js hero deleted | Photographic plate outperformed all three generative attempts; also fixed scroll jank | Crystal+rings (v1), glass monolith (v2), procedural shatter (v2.5) |
| 2026-07-27 | Typography: Cormorant Garamond display / Archivo+Plex Sans body / Plex Mono utility | v3 design language; certificate/engraved register fits a signing firm | Satoshi (removed), Wix Madefor as display |
| 2026-07-28 | Site-wide palette: noir/linen/fog/brass tokens; navy retired from UI | User: "match the hero to all the others"; seams between navy chrome and noir hero read as glitch | Keeping navy navbar/footer |
| 2026-07-28 | Trusted-by logo lane kept despite §14 marquee rejection | User-directed feature with spotlight interaction; flagged for post-launch review | Static logo grid |
| 2026-07-29 | §1 amended: two spectacle moments per page allowed, never sharing a viewport | User asked for a "motion-full, highly interactive, connective 3D storytelling" site. Flagged the conflict with the one-spectacle rule before building; user chose to add one 3D narrative spine rather than a full 3D rebuild | Full 3D across all pages (rejected — fails the §13 substitution test and the credibility register); motion-only with no new 3D |
| 2026-07-29 | §2.2 amended: three.js permitted in a second place — the homepage valuation journey | It is product-truth, not decoration: the firm's own five process steps, one point field re-formed. Lazy + desktop-gated, so the homepage's first-load JS moves 285→288KB gz | A WebGL hero (still rejected — the 2026-07-27 decision stands) |
| 2026-07-29 | `OurProcess.jsx` deleted, replaced by `ValuationJourney.jsx` | The 3D spine narrates the same five steps with the same copy; keeping both would have shipped the process twice | Adding the journey alongside OurProcess |
| 2026-07-29 | Survey thread added as the homepage's connective element | Gives the page one continuous spine and plants the beacon metaphor the Locations map journey pays off | Per-section progress indicators |
| 2026-07-29 | "Entropy Resolved" built as a **candidate at `/studio`**, not as a replacement for `/` | Signature element = a persistent particle-silk ribbon with scroll-driven states; monochrome palette; roman/italic display mixing. Built to `docs/entropy-resolved-brief.md`. Kept on its own route so the two directions can be judged side by side before anything is retired, and because `/` is not yet pushed anywhere | Replacing `/` outright (deferred until compared) |
| 2026-07-29 | Studio page ships **monochrome**; the champagne accent is used only on the process progress hairline, not in the ribbon | The brief's own tiebreak ("if in doubt, ship pure monochrome"). Gold at the low alphas additive blending needs read as brown dust on black, not silk | Tinting the glow with `--brass`/champagne (tried, rejected on sight) |
| 2026-07-29 | Studio keeps the site's existing Cormorant / IBM Plex Sans / IBM Plex Mono rather than adding Fraunces + Instrument Sans | The brief lists Cormorant as an accepted display face, and body copy is rare on the page. A fourth and fifth family would cost first-load weight for two paragraphs — §2.3 floor wins over the font suggestion | Loading Instrument Sans for body |
| 2026-07-29 | Studio hero headline floor lowered to 46px (brief said 72px min) | At 390px, 72px wraps the headline to six lines and pushes the body copy off-screen. Readability floor wins; the clamp still reaches the brief's 176px ceiling on desktop | Keeping the 72px floor |
| 2026-07-29 | Studio reuses the global Lenis instance (lerp 0.1) instead of its own at 0.09 | Two Lenis instances on one document fight each other. The 0.01 difference is not perceptible | A page-local Lenis |
| open | **60fps floor on the studio page is UNVERIFIED** | This container renders WebGL through SwiftShader (software). Measured 28fps on `/studio` against 14fps on `/` with no WebGL on screen — i.e. the environment is the bottleneck and the numbers say nothing about real hardware. Must be re-measured on a real GPU and a mid-tier Android before this page ships. DPR cap, per-breakpoint counts, single draw call, idle-mount and hidden-tab pause are all in place | — |
| 2026-07-29 | §5.6 IDENTITY beat added to `/studio`: a pinned horizontal track whose four slides the particle field condenses into, smearing with track velocity and springing back at rest | Signature element = persistent particle-silk ribbon with scroll-driven states; monochrome palette; roman/italic display mixing. The beat performs the brand argument — scatter in motion, a defensible position at rest. Same Points object as the ribbon, blended by a mode uniform; never a second particle system | Mounting a second system for the slides (rejected — §2.3 and the whole point of one field) |
| 2026-07-29 | Studio pin budget is now exactly two: the method beat and the identity beat | Matches the brief's own ceiling. Nothing else on that page may pin | — |
| 2026-07-29 | §3.5 cursor repulsion added — particles spread from the pointer and flow back | The one permitted cursor interaction per the brief's drift guards. Two uniforms and ~6 shader ops, quadratic falloff, displacement capped well under the repel radius so the silhouette always stays readable. Disabled on touch and under reduced motion | Cursor trails or pointer-attached particles (both on the rejection list) |
| 2026-07-29 | **Canvas sizing bug fixed** — `renderer.setSize(w, h, false)` in `studioScene.js` | With `updateStyle` off, three.js leaves the canvas unsized in CSS so it displays at its drawing-buffer size. At DPR 1.75 that is a 2240×1260 element in a 1280×720 window, overflowing 960px right and 540px down and putting the scene's centre near 87%/87%. **Invisible at DPR 1**, which is what this container renders at — so repeated "the particles are on the right" reports measured as centred here and no camera change could ever have fixed it. Verification must set `deviceScaleFactor` to match the reporter's device | — |
| open | consulting.jpg / law.jpg replacement | Awaiting real photography from the firm | — |
| open | `/locations` ships three.js in first-load JS (399KB gz, pre-existing) | `MapJourney` imports `lib/map3d` statically — should adopt `ValuationJourney`'s dynamic-import pattern | — |
| open | Homepage first-load JS is ~38KB gz over the §2.3 budget, inherited from before this work | Chase `motion` / `lucide-react` / `gsap` import surface | — |
| open | Case studies content | No real assignment data provided yet — section renders nothing | Fabricated examples (refused) |
| open | Production domain + Formspree endpoint | Placeholders still in metadata/contact form | — |
| 2026-07-29 | Studio particle field re-tinted to a three-stop silver: `#E4E4E0` core / `#B9B9B4` body / `#6E6E68` gauze. Zero warm bias anywhere in the shader | The warm stop (`#FFD7A0`) read as yellow dust under additive blending rather than as gold, and it competed with the gold typographic rule for the same accent job. Gold now survives only outside the canvas — section-headline rules and hairline hover states. Colour-only: count, motion, undulation, scroll states, cursor repulsion and the reduced-motion frame are untouched. Verified by sampling every lit pixel (V > 0.10) at 1440px, 390px and under reduced motion: **0 pixels** with hue 30°–60° at S > 20% | Keeping the steel/gold two-tone; desaturating the warm stop in place (still left a hue at low S) |
| 2026-07-29 | §5.6 IDENTITY's four abstract slides **cut**; the horizontal beat now carries the trusted-by strip. `StudioIdentity.jsx` deleted, `StudioTrusted.jsx` in its place | The mechanic was doing real work but arguing with copy ("We don't estimate value.") that the page already makes better elsewhere. Pointed at the fifteen real institutions, the same travel carries evidence instead of assertion. Still one pass driven purely by scroll position — no autoplay, no loop, no duplicated set, so §14's marquee rejection is not reopened (verified: 15 cards, 0.0px drift when idle). Pin budget unchanged at two | Keeping the slides and adding a separate logo section (a third pin, over budget) |
| 2026-07-29 | Studio reverses its "names only, no logos" decision of earlier today | That decision reasoned the marks are colour artwork on a monochrome page. With the field now silver (above), the plates are the only colour on the page — which is the argument, not a violation of it: these are other people's marks, reproduced as issued. Cards are opaque white and full colour inside the central 60%, easing to 55% opacity / 96% scale at the edges. Opacity and transform only — no grayscale filter animation | A monochrome or duotone treatment of the logos (misrepresents other organisations' marks) |
| 2026-07-29 | The band is measured against the **pin**, not the viewport | Anchors are taken on mount, when the section is ~7,400px below the fold, so a viewport-relative rect put the band that far off the bottom of frame. The pin is exactly one viewport tall and full width, so its box is the frame the cards will actually be seen in. Same class of bug as the DPR one above: a measurement that is correct in the moment it is taken and wrong for the moment it describes | — |
| 2026-07-29 | `fieldBus` gains `onField(cb)` | The scene mounts on `requestIdleCallback`, so a section that needs to hand it DOM measurements is routinely ready first — the band's anchors were being computed against a field that did not exist yet and silently discarded. One callback, fired once on attach | Polling for the field; measuring on a timeout |
| open | The trusted-by progress counter reads `07 Institutions`, per the change request, while the page's own eyebrows number this section 03 | Flagged rather than silently renumbered — the counter is verbatim from the brief and the section eyebrows are the page's existing scheme. One of the two should move before launch | — |
| 2026-07-29 | §5.7 THE DELIVERABLE added to `/studio`: a physical A4 sheet, section-scoped light palette (`--paper #EFEEEA` / `--paper-edge #D8D6CF` / `--paper-ink #1A1A18`) against the page's near-black | The page argues that the firm's product is a defensible document, and until now never showed one. The sheet is the only opaque, light, shadowed object on the page — deliberately outside the page's own visual language, because it is the thing that leaves the building. Two-layer shadow (cast + contact), entry rises 48px over 0.9s with the shadow lengthening in sync, rotateX 4°→0° across the section's scroll. No pin — the budget stays at two. Nothing glassy, glowing or gold | A rendered 3D page in the particle field (the method beat already does that); a photograph (none exists yet) |
| 2026-07-29 | Field gains a static rectangular exclusion mask (`uPlate`) so points part around the sheet | A sheet the particles shine through is a texture, not an object. Signed distance to the rect, push along its gradient, ~80px feather — and each point carries its own feather multiplier, because one shared distance stacks every displaced dot at the same offset and draws a hard wall around the paper. Cursor repulsion (§3.5) stays live outside the mask | Rendering the sheet into the WebGL scene; a CSS backdrop that hides the field wholesale |
| open | **`TODO(asset): real report sample`** — the sheet carries structure only | Letterhead, redaction bars, an empty schedule of figures, an ink stroke that is not anyone's signature, and `Ref. SSA/—/——`. No figure, name, or reference on it states anything about a real valuation. Replace with a redacted page from an actual engagement when the firm supplies one | Inventing plausible report contents (refused — §2 ground rule) |
| 2026-07-29 | **Final pass on the three changes, measured** | 390px / 768px / 1440px and `prefers-reduced-motion` at 1440px and 390px, five sections each. No horizontal page overflow at any width. Pin spacers: 2 at 1440 (method + trusted), 1 at 768, 0 at 390 and under reduced motion — the pin budget holds. Zero page or console errors in any run. Yellow audit (hue 30°–60°, S > 20%, V ≥ 0.10) over hero, method and CTA: **0 pixels** at every width; the trusted and paper sections are excluded from that audit because the logo plates and the sheet are legitimately coloured DOM, not field. First-load JS at the `load` event, production build, measured against `40550ed`: `/studio` **188 → 190KB** on the wire, `/` unchanged at 249–250KB | — |
| open | **Lighthouse throttled-mobile was not run** | Not installed, and adding it would breach the change request's no-new-dependencies rule. More to the point it would not mean anything here: this container renders WebGL through SwiftShader, which is already logged above as the reason the 60fps floor is unverified. A Lighthouse score from this environment would read as evidence and be worth nothing. Must be run on real hardware alongside the fps measurement | Installing Lighthouse and reporting the number anyway |
| 2026-07-30 | **"Particles multiply on reverse scroll" — real bug, wrong diagnosis.** Cause: `velTarget` stuck at −1 | Reported as the particle count growing until the hero was unreadable. Measured: the count never changes and every scroll-driven uniform returns exactly. What did not return was the velocity reading. `StudioTrusted` writes `setVelocity(scrollVelocity)` each frame while pinned; scroll up out of it quickly and the last value written is a large negative one with no further update coming. The shader scales point size by `1 + abs(uVel) * 1.5` and glow by `1 + abs(uVel) * 0.35`, so every dot rendered **2.5× its size, permanently** — 2.23× the lit pixels over the hero copy. Fixed at the source: velocity now lapses after 140ms unless something restates it, so it comes to rest on every scroll path rather than relying on each call site to remember a reset. Measured 2.23× → **0.97×** | Adding `setVelocity(0)` to each trigger's exit (fixes today's path, not tomorrow's) |
| 2026-07-30 | The prescribed fix for that bug did not apply | The brief asked for a fixed `PARTICLE_COUNT`, no re-instantiation, symmetric interpolation and proper disposal. The field already had all four — one `Points` object, one canvas, positions lerped from a single scroll value. Rebuilding to that spec would have changed nothing. Recorded because the symptom genuinely looked like accumulation | — |
| 2026-07-30 | Ribbon spin pinned to a fixed angle once the cloth has formed | Secondary hardening found while chasing the above, **not** the reported bug. `spin.rotation.y` was a bare time accumulator, so the torus's presentation at the hero — edge-on and narrow, or open and wide — depended only on how long you had been on the page. The funnel still whirls as it forms; after that the angle is fixed, which makes "returning to the hero reproduces the original formation" literally true. The cloth's life comes from the noise term and per-point wander, not from axial spin | Bounding the spin to a slow arc (tried at ±0.3rad — wide enough to swing the torus open by itself) |
| 2026-07-30 | Cursor repulsion radius 0.42 → 1.22 (~60px → ~180px), strength 0.19 → 0.38 | Reported as "hover does nothing". It was working — `uMouse` tracked and the strength ramped — but it was displacing a 60px disc, which is invisible on a 1440px field. Now measured at the cursor rather than across the frame: density in the inner 60px drops **7.6%**, tapering to 0 by 180px. Max displacement stays at half the radius, so the silhouette still reads. On `/` there is genuinely no repulsion — `reportScene.js` never had it | Raising strength further (0.55 punches a hole rather than opening a crater) |
| 2026-07-30 | §5.7 THE DELIVERABLE removed; its CTA moved into the hero | User request, one day after it was built. The primary CTA now appears above the fold and in the closing block, so it is not lost with the section. `StudioPaper.jsx` and its CSS deleted; the field's `uPlate` exclusion mask stays in the scene, unused but harmless, for whatever next wants to part the field around a solid object | Leaving the CTA only in the closing block |
| 2026-07-30 | Studio certificate gains the **page border and the seal** it was missing | The homepage's certificate (`reportScene.js`) draws both as line and mesh objects; the studio's `buildPage` was text rows only. Rebuilt as points, since the studio page has exactly one particle system and nothing else may draw: a double inset rule around the sheet, and a seal of three concentric rules with a guilloche of 44 radial ticks. Weights are deliberately light — at 3.2 the frame took so large a share of the fixed count that its edges rendered as solid bands and outshone the prose they framed | Adding a second scene or a DOM overlay for the frame |
| 2026-07-30 | Certificate text thinned: 15 rows at 0.9 spacing → 10 at 1.24, jitter 0.06/0.08 → 0.04/0.05 | It read as one illegible mass rather than lines on a page. The count is fixed, so the fix is threefold: fewer rows, tighter jitter so points sit **on** a line instead of in a fuzzy band around it, and the new frame and seal absorbing points that were crowding the prose. Page-beat camera pulled back 6.0 → 6.9 so the sheet no longer clips at the top of the frame | — |
| 2026-07-30 | The funnel is now the index section's beat — "What we are asked to value." | The tornado's job is to make that section arrive: it is the page's list of what the firm actually does and it was the one substantial section the field ignored. As the section crosses the viewport the cloth unwinds into the funnel and settles again on the way out, and holding any row — pointer or keyboard — makes the funnel turn harder and lift in brightness. **No pin**: the whole beat is a function of how far the section has crossed the frame, so it scrubs backwards exactly and the budget stays at two. Behaviour attaches to markup the server already rendered, so the rows keep working without JS | A third pinned section (over the §1 budget); a separate tornado section (the rows are the content the funnel is about) |
| 2026-07-30 | `.er-indexscrim` added — a left-weighted wash under the index rows | A point field over serif titles fails the §2.4 contrast floor. The wash is heaviest under the type column and clears by 78% of the width, so the copy is readable and the right of the frame stays open for the funnel | Dimming the field globally during the beat (costs the effect everywhere) |
| 2026-07-31 | **WI-1** — index rows get one colour world per service | Six hues (`--hue-01..06`), all at L 38–45 and low chroma so moving between rows reads as turning one dial. Reaches three places: the particle field, an ambient wash, and the row's own furniture. Exactly one row active at a time; pointer and keyboard activate fully (uTint 0.55), scrolling with the pointer away activates the centre-crossing row at half strength (0.28, colour only). Row-to-row crossfades hue-to-hue without dipping through neutral, because the amount holds while only the hue lerps. Measured at 1440: field dominant hue **352** while row 03 is held (oxblood is 354) | — |
| 2026-07-31 | Hue applied as a luminance-preserving shift, weighted **down** as brightness rises | The hue is rescaled to the grey it replaces, so tinting moves chroma and never brightness and the silver ramp survives underneath; the weight falls as `vTone` rises so the gauze carries the colour and the brightest filaments stay near-white. That is the difference between grey dust catching coloured light and dyed confetti | A flat `mix()` to the hue (flattens the ramp); multiplying the hue in (clips the cores) |
| 2026-07-31 | `setTint` **lapses** rather than being reset by callers | The brief asked for an assertion that uTint rests at 0 outside the index. Made structural instead, on the pattern that fixed the velocity bug: the tint decays unless something restates it, and `StudioIndex` only restates it while a row is lit — so stopping the loop *is* the fade. A dev-mode warning fires if it lapses while still requested. Verified: hero dominant hue **214 before and 214 after** visiting the index, Δ0, at every breakpoint | A boolean assertion that reports the leak after it happens |
| 2026-07-31 | Index numbers use the hue **lifted toward white**, not raw | The palette sits at L 38–45 by design; used raw on near-black the number lands at **2.37:1** and fails the 4.5:1 floor the same brief sets. `color-mix(… 55%, #fff)` keeps the hue unmistakable and measures **6.99–8.82:1** across all six. Description text 6.67:1 | Brightening the palette (breaks the single tonal band) |
| 2026-07-31 | **Sanctioned deviation** — the ambient wash reopens §5's ban on decorative background gradients | Allowed as functional state signalling only: scoped to this section, one element, resting at zero opacity, capped at 14%, animated by transform and opacity alone. Every contrast floor still passes underneath it | — |
| 2026-07-31 | **WI-2** — right-side descriptions replace the mono tags on activate | Tag and description share one absolutely-positioned slot with a reserved min-height, so activating a row can never reflow it or change its height. Measured at 390/768/1440: **no height change on any row**. Tags ease out at 250ms, description eases in from the right at 350ms on a 60ms delay; scroll-soft activation deliberately does not bring the description. Touch: first tap activates, second follows the link, rows stay real links | — |
| 2026-07-31 | **WI-3** — loader normalised to flat `#050505` | It was `linear-gradient(--bg-lift → --bg-deep)`, and `--bg-lift` is a blue-grey — the load screen measured at hue 210. Now **0 coloured pixels**. The trusted counter was the hard-coded `07 Institutions` flagged as open on 29 Jul; it now reads the centred card against the real list length (`04 / 15 Institutions`), so it tracks the list instead of drifting the next time one is added. Drag-smear measured **+12.6%** particle coverage during travel with a **636ms** spring-back, inside the 0.8s cap | — |
| 2026-07-31 | **WI-4** — repulsion displacement capped structurally at 0.5× radius | Unclamped it worked out to **0.498×** (strength 0.38 × aDrag's 1.6 ceiling against a 1.22 radius) — inside the limit, but by arithmetic coincidence, and any retune of either term would silently punch holes through the tornado and the certificate. Capped the displacement, not the radius, per the brief | Reducing the radius (loses the reach that made hover visible) |
| 2026-07-31 | Certificate-as-particles logged as an **accepted deviation** from the solid-paper spec | Per v3: the deliverable is rendered inside the particle system for world-coherence — every object on the site is made of measurements. The solid warm-white sheet is not to be built; `StudioPaper.jsx` stays deleted | The solid A4 sheet (built 29 Jul, removed 30 Jul) |
| open | **Lighthouse throttled mobile still not run** | Unchanged from 29 Jul: not installed, and this container renders WebGL through SwiftShader, so any score would measure the environment rather than the page. Must be run on real hardware alongside the unverified 60fps floor | — |
| 2026-07-31 | **WI-5** — the field is capped and masked so type wins over it | Glyph-against-particle contrast in the hero measured 1.5:1 against a 4.5:1 floor, while the particles ran at 13.5:1 against the background — foreground brightness for a background element. Two mechanisms, both inside the scene: a per-section brightness cap (`uCap`, 0.42 where copy sits over the field, 1.0 where none does — the certificate, the funnel and the trusted band carry no copy across them), and text-block masks (`uText[4]`) that dim points to 35% and shrink them to 60% inside a heading or paragraph's box, feathered 60px. Measured after: field p95 in the headline band **L34 at 1440 / L23 at 390** against the ≤L120 cap, and **1.77% / 1.10%** of the band above L90 against the ≤2% ceiling | — |
| 2026-07-31 | The text masks **dim** rather than displace, unlike the certificate's | The plate mask pushes points aside, which is right for a solid object. Doing that to a paragraph would leave a hole shaped like the paragraph and break the field's continuity. Dimming keeps the field unbroken and simply quietens the air the type sits in — the same rectangle SDF, a different response | Reusing the plate's displacement (leaves paragraph-shaped voids) |
| 2026-07-31 | Display serif raised from wght 300 to **420**, tracking −0.015em → −0.01em | The light cut of a high-contrast serif could not survive residual dots behind it — the hairlines simply disappeared. Same family, same italic, moved on the variable weight axis, with the tracking opened to carry the extra mass. Body over the canvas goes 16px → 18px at `#B5AFA5`; body on flat black is unchanged | Switching typeface (banned); changing the ink colour (already near-white) |
| 2026-07-31 | `.er-indexscrim` **removed** — no scrim divs | Added 30 Jul to keep the index rows legible under the funnel. WI-5 rules scrims out, and the text-block masks do the same job inside the field where it belongs. The ambient wash stays: it is state signalling, not a legibility patch | Keeping both (the scrim would fight the wash for the same pixels) |
| 2026-07-31 | **Two-voice type system: serif speaks, mono measures.** The grotesk body family is gone from `/studio` | Every non-display role moves to the utility mono the navbar already used — body copy, sub-lines, index tags and hover descriptions, client quotes, stats figures, CTAs, footer. Running text is sentence case at 15–16px / 1.7 with no letterspacing; tracked uppercase (0.18–0.25em) stays reserved for labels of four words or fewer. Audited on the live page: **122 mono / 42 serif elements, 0 grotesk**, with the serif confined to `.er-display`, `.er-row__t`, `.er-wordmark` and `.er-bigmark` — headlines and the two wordmarks, exactly as locked | Keeping the grotesk for prose (a third voice the system does not need) |
| 2026-07-31 | Journey numerals, proof figures and client quotes moved off the serif — **a judgement call worth flagging** | The rule says the serif is for headlines and wordmarks only, and none of these three is a headline. Numerals in mono suit the principle outright — they are the parts of the page that measure. The client quotes are the arguable one: a pull-quote is conventionally a display role, and setting testimony in mono reads as transcript rather than voice. Shipped per the locked rule; one line to revert if the register is wrong | Leaving all three on the serif (contradicts the lock) |
| 2026-07-31 | WI-5.5 revised: sub-text over the canvas stays **16px**, only the tone lifts | The 30 Jul pass had taken it to 18px while it was still grotesk. Mono runs wider at the same nominal size, so the bump is no longer needed — `#B5AFA5` over the field, standard muted ink on flat black | — |
| 2026-07-31 | `data-quiet` extended from two blocks to **every section heading** | The cap only engages while a text block is on screen, so sections without a marked heading were still burning at full brightness — the client quotes sat over an uncapped funnel and their attributions were unreadable. Also fixed a real bug in the mechanism: `measure()` sliced to the first four blocks in DOM order, so the later sections' headings never entered the list at all. All blocks are measured now and the choice of four happens at push time, where it can mean "the ones actually on screen" | — |
| 2026-07-31 | **WI-6 closing pass** | 390 / 768 / 1440 plus reduced motion at two widths. Headline band: field p95 **L33 / L23** against the ≤L120 cap, **1.76% / 1.12%** above L90 against the ≤2% ceiling. No horizontal overflow anywhere, pin budget holds at two, no page or console errors. Tint rests at 0 outside the index at every width (hero hue 214 before and after, Δ0). Counter live at `04 / 15 Institutions` | — |
| open | The grotesk is still loaded in `layout.js` for the rest of the site | `/`, `/about`, `/services` and the others still set body copy in IBM Plex Sans. Removing it globally is outside this request's scope ("touch only the items in WORK ITEMS"), so it stays until the two-voice system is adopted site-wide | — |
| 2026-07-31 | **Two-voice system: serif speaks, mono measures. Mono upgraded to Geist Mono** | Variable 100–900, SIL-licensed, self-hosted by next/font at weights 400 and 500 only — every weight the system actually uses. IBM Plex Mono stays in the stack as fallback while the rest of the site migrates. Verified on the live page: the resolved family for `.er-body` is **Geist Mono** | Fragment Mono (warmer, kept as the approved alternative); Berkeley Mono (needs a $75 licence, not confirmed) |
| 2026-07-31 | **WI-6 — warm graphite replaces near-black site-wide on `/studio`** | `--bg #151515`, `--bg-deep #0F0F10` (hero + footer), `--surface #1C1C1E`. Near-black is the harshest choice on an emissive screen — it maximises the halo an astigmatic reader sees around light type. Flat fills only: the old `.er` gradient is gone. Contrast re-verified on the real surfaces: ink **15.7**, ink-dim **6.43**, ink on deep **16.47**, ink-dim on surface **5.99**, over-canvas body **8.79** | — |
| 2026-07-31 | The canvas clear colour is now **transparent**, which is what made the retheme possible at all | The canvas is fixed across the whole viewport, so its opaque clear colour *was* the site's background — no CSS level could ever have been seen behind it, and "sections switch levels at hairlines" was unimplementable. Cleared to alpha 0 so the page's own levels show through; additive blending composites correctly over it. Section fills sit at `z-index: -1` inside `.er`'s stacking context, behind the canvas but above the page fill — putting a fill straight on `.er-hero` would have hidden the field | Keeping the clear colour opaque and painting levels into it (only one colour is possible) |
| 2026-07-31 | `--ink-faint` lifted `#5A626C → #8B8B90` | Measured **2.96:1** on the new graphite, failing the 4.5 floor WI-6 sets for every text role. Now **5.39:1**. `--ink` and `--ink-dim` measured 15.7 and 6.43 and are unchanged, as specified | — |
| 2026-07-31 | Field brightness ceiling lowered ~10% for graphite | On a lifted background the same field reads brighter, so `uCap` tops out at 0.9 rather than 1.0 to hold the same perceived glow. Headline band after the retheme: p95 **L34 / L26**, **1.74% / 1.13%** above L90 — still inside the WI-5 acceptance | — |
| 2026-07-31 | **WI-7 — the clients river.** Sanctioned exception to the auto-motion ban | Auto-motion is banned for decorative marquees. This is not one: it is content the visitor is meant to graze, it drifts slower than reading speed, it stops when looked at, and under reduced motion it does not move. Three columns at ≥1440px (two below, one below 700px), loops of **28s / 34s / 31s** so they never synchronise, one transform per column, duplicate set `aria-hidden` so each quote is announced once. Measured: **9.3px/s** against the 30px/s cap, **0.0px** of movement while hovered, static grid under reduced motion | A horizontal ticker (fights the page's own scroll axis) |
| 2026-07-31 | River drops to **one column below 700px**, against the spec's two | Two columns of testimonials at 390px would be ~170px wide — narrower than the quotes need. Deviation flagged rather than shipped silently | — |
| open | **TODO(content): only four reviews exist in the repo** | The river is built to take the firm's full list and redistributes round-robin as entries are added. Four is thin for a section whose point is volume | Duplicating the four to fill the columns (would fabricate testimony) |
| 2026-07-31 | **WI-8 — the Record assembles one piece at a time** | Eyebrow tracks in, headline line-masks, then each stat column builds from its rule upward (rule draws → label → figure mask-reveals → caption) 140ms apart, then the institutions cascade at 60ms. No count-up ticker: a ticker turns a measurement into a slot machine. Fires once; a guard trigger force-completes the timeline on section exit so scrolling past fast can never strand a half-revealed row. Verified at 1440/390/reduced: **0 unrevealed elements, 0 undrawn rules** after settle. `StudioClient`'s generic reveal skips the section so the two cannot fight over the same elements | — |
| 2026-07-31 | **WI-9 closing pass** | 390 / 768 / 1440 plus reduced motion at two widths. No horizontal overflow, pin budget holds at two, no page or console errors, tint rests at 0 outside the index (hero hue 215 before and after, Δ0 at every width), counter live at `04 / 15 Institutions` | — |
| 2026-07-31 | **WI-1 — the index becomes a pinned horizontal service showcase** | Six full-viewport slides, 600svh section, `translateX` 0 → −500vw on `scrub: 1` with a gentle 1/5 snap. Header fixed at the top of the pin; only the track travels. Each slide carries the mono index, display-serif title, mono description, tag row and a hairline `EXPLORE THIS SERVICE →` link, against a 4:5 image card with inner parallax. Everything moves on transform or opacity from a single scrub value — no layout reads in the handler. Verified: **2 pins**, 600svh (5400px at vh 900), 6 slides, track 8640px, counter and hue advancing `02→04→06` across the traverse | The vertical row list (kept as the phone and reduced-motion fallback) |
| 2026-07-31 | Colour worlds re-keyed from hover to **centeredness** | The old spec needed hover logic, a 300ms grace and a scroll-soft tier to decide which row was active. In the showcase the central slide simply *is* the active one, so hue intensity is a function of one number — and dragging between slides mixes the two adjacent hues without ever dipping through grey, for free. `uTint` peaks at 0.45 and still rests at 0 outside the section via the lapse | — |
| 2026-07-31 | **Pin budget resolution: TRUSTED gives up its pin** | The page keeps exactly two — PROCESS and the showcase. The strip becomes a native scroller at every width (`overflow-x` + scroll-snap, throwable by finger, trackpad or shift-wheel), with edge-fade masks so it visibly continues off-canvas and velocity coupling re-keyed to its own scroll. Logo cards do not need cinematic scrubbing; the showcase does. Verified: pins 2 / 1 / 0 at 1440 / 768 / 390 | — |
| 2026-07-31 | Two cascade bugs found while wiring the fallback | The stacked list's own `display: grid` is declared after the media query that hid it, so at equal specificity it won and the list rendered *underneath* the pinned showcase at desktop. Hiding it unscoped then broke the opposite case — under reduced motion there is no pinned track and the stack IS the presentation, so the section vanished entirely at 1440. The rule is now scoped to `.er-scwrap + .er-scstack`, which is true only when a pinned track exists | — |
| 2026-07-31 | **WI-2 — the image cards ship, the photographs do not** | The card, mask, inner parallax and the shared grade are all built and working; the six slots render as marked `TODO(asset)` placeholders at final dimensions in the final grade. WI-2's interim state permits licensed editorial photography, but a licence must be held and recorded — and there is no licensed source here. Pulling images off the web would put unlicensed work into a commercial site and fabricate the provenance `docs/asset-licenses.md` exists to record. The repo's own photography does not cover it: three of seven files are stock people (banned by the art direction), one is the logo, and two are already logged as placeholder stock awaiting replacement. Swapping real exports in is a data change | Sourcing unlicensed stock (refused); reusing the banned people photography |
| open | **Showcase snap needs confirming on a real device** | With a programmatic 50-step scroll the snap settles a slide further along than the scroll position implies — ScrollTrigger reads the synthetic scroll's velocity and carries forward. Probably an artefact of driving the scroll from script rather than a wheel, but WI-1 says smoothness outranks snapping: if it fights the drag on real hardware, remove the `snap` block | — |
| 2026-08-01 | **v3 re-verification pass — no code changes.** The change request was received a second time, byte-identical to the 31 Jul copy | WI-1…WI-9 were all delivered across `090a1e5`, `6063810`, `9c4754b`, `ce8b7cf`, `3361eb5`. Re-running the acceptance suite against the current build rather than re-executing it, because every item is either done or explicitly regression-guarded and re-running a build step can only put a VERIFIED item at risk. Measured on `3361eb5`: pins **2 / 1 / 0** at 1440/768/390 (budget holds); no horizontal overflow at any width; **no page or console errors** in any of four modes. Tokens `#151515 / #0f0f10 / #1c1c1e`, resolved mono **Geist Mono**; contrast ink **15.7**, ink-dim **6.43**, ink-faint **5.39**, ink on deep **16.47**, ink-dim on surface **5.99**, over-canvas body **8.79** — all ≥ 4.5. Headline band p95 **L34 / L26** against the ≤L120 cap, **1.74% / 1.13%** above L90 against the ≤2% ceiling. Showcase 600svh, 6 slides, 8640px track, counter and hue advancing across the traverse. River 3/2 columns at 28/34/31s, **9.0px/s** against the 30px/s cap, **0.0px** while hovered, static under reduced motion. Record settles with **0 unrevealed elements, 0 undrawn rules**. WI-9 screenshot set captured at 390/768/1440 + reduced: hero, showcase, trusted, certificate, river, record mid-reveal | Re-executing the work items (would churn regression-guarded code for no gain) |
| 2026-08-01 | **Both strips drift on their own again — correcting an over-literal reading of the unpin** | WI-1's pin-budget resolution said the trusted strip "gives up its pin and converts to an unpinned, natively scrollable strip". I removed the pin *and* the motion, leaving a lane the visitor had to drag — which the client never asked for and immediately flagged. The pin stays gone, so the budget still holds at two; the strip now drifts continuously under one transform, the way the homepage's marquee does. Measured **78px/s** at 390 and **39px/s** at 1440, slowing to **10–22px/s** on hover and never stopping | Restoring the pin (would break the budget the showcase needs) |
| 2026-08-01 | Clients river re-cut from vertical columns to **horizontal lanes**, matching the homepage | WI-7 specified vertical drift, reasoning that a horizontal ticker fights the page's scroll axis. The client's direction supersedes it: the reviews should move "just like in the homepage". Two lanes running in opposite directions at 46 and 38px/s so the pair never reads as one block, set rendered twice with the duplicate `aria-hidden`, slowing to ~12px/s on hover. Measured **21.9 / 46.4px/s** drifting, **7.4 / 12.1px/s** hovered. Static under reduced motion, unchanged | Keeping the vertical drift (correct to the written spec, wrong to the client's intent) |
| 2026-08-01 | The trusted strip's progress hairline and `NN / 15` counter removed | Both belonged to a traverse that had a start and an end. A continuous drift has neither, so a progress bar would be lying about something. The count remains as `15 Institutions`, which is the part that was actually saying something | — |
| 2026-08-01 | **The six showcase photographs land — WI-2 closes** | Client-supplied, already graded to the art direction (desaturated, graphite-and-silver duotone on the site tokens, grain, blacks crushed, slide hue in the shadows at 8%), with full provenance: Unsplash License, photographer and source ID per image, recorded in `docs/asset-licenses.md`. Served as 1600×2000 WebP resized per breakpoint by `next/image`, loaded eagerly so no card is still decoding when the section pins. Measured: **6/6 decoded at every breakpoint, largest response 78KB** against the ≤200KB budget, natural 519×649 against a rendered 518×646 — exact display size | — |
| 2026-08-01 | **No CSS grade on the photographs** | The build's own duotone/desaturate/grain pass was removed when the images arrived: the grade is baked into the exports, and a second pass in CSS stacks on the first and crushes what the first resolved. The card now contributes the mask, the parallax and the shadow only | Keeping both grades |
| 2026-08-01 | Slide 02 ships as a **flagged candidate** | The supplied file is a vintage radial aircraft engine — texture-forward, correct to the grade, and materially better than an empty slot, but an aeroplane engine is not the industrial plant this firm values. The supplier marked it PLACEHOLDER and that marker is carried into the repo's licence table rather than quietly dropped | Holding the whole set back for one image |
| 2026-08-01 | **The drift reintroduced the velocity-stick bug from the other end** | Found while measuring the typography acceptance after adding the images: the headline band had gone from 1.74% to **2.01%** above L90, over the ≤2% ceiling, with p95 L34→L45. Not the images. The strip's drift rAF runs for the page's whole life, and it was writing `setVelocity` every frame — pinning uVel at ~0.16 site-wide, so every dot on every section rendered fatter and brighter, hero included. Exactly the failure fixed on 30 Jul, recreated from the opposite direction: then nothing reset it, now something never stopped setting it. Gated to its own section's `isActive`; band back to **L35 / 1.75%**. Note for the next such coupling: a continuous animation that speaks to shared state must be scoped to where it is visible, not to where it is mounted | — |
| 2026-08-01 | Stacked showcase card capped at 460px | Under reduced motion at 1440 the card stretched to the full measure, and a 4:5 mask that wide becomes a wall with `object-fit` cropping the subject to a letterbox | — |
| 2026-08-01 | **DESIGN LOCK — final polish pass, P-1…P-6.** From here the studio page's visual language is fixed | The six items below close the client's last open notes. Everything under the pass's regression guard — the particle system and its formations, scroll states, cursor repulsion, showcase mechanics/images/colour worlds/counter, the graphite tokens, the trusted strip and the record stagger — was left untouched | — |
| 2026-08-01 | **P-1 — the hero loses its furniture; the nav links move behind a hamburger** | Deleted: the `SCROLL TO DESCEND` cue with its pulse line, and both hero satellites (`NAGPUR · CHHINDWARA`, `IBBI · INCOME TAX DEPT.`). The links now live behind a 24×16px three-hairline icon, right-aligned, wordmark still top-left. Hover reveals them sliding leftward, staggered 40ms over 300ms on the entrance curve, held while the pointer is over icon or links and eased out 200ms after leave. The outer lines part 2px on open — no X-morph. Scrolled hide-on-down/show-on-up unchanged | A CSS-only `:hover` reveal (leaves touch and keyboard with no way in) |
| 2026-08-01 | P-1's icon is a real `<button aria-expanded>`; hover is only an enhancement | Click and tap toggle it, keyboard focus opens it, Escape closes it and returns focus to the button. Verified at 1440 and 390: click `aria-expanded` false→true, Escape → false, programmatic focus → true, cold hover → true, pointer leave → false | — |
| 2026-08-01 | Two P-1 defects found in verification, both fixed | (1) The closed link list still claimed its full width in flow, which pushed the icon itself **off the right edge at 390px** — nothing to tap. The list is now absolutely positioned, so the wrap is exactly the icon (measured 285→366px inside a 390px viewport). (2) `aria-expanded` never moved on click: a mouse click fires `focusin` first, and the focus handler and the click toggle cancelled each other out. Focus now opens only when it is *keyboard* focus (`:focus-visible`), and `aria-expanded` reports whether the links are actually reachable rather than tracking the click state alone — it read "false" while the menu stood open on hover | — |
| 2026-08-01 | Below 640px the P-1 menu drops **under** the bar instead of sliding out beside it | Four tracked-caps links do not fit next to a 24px icon on a phone. Same button, same states, same `aria-expanded`; only the axis changes | Shrinking the links (would break the label scale that holds everywhere else) |
| 2026-08-01 | **P-2 — the mono becomes the display voice; the serif keeps exactly two places** | Client decision, locked. Geist Mono at 600 for section headers, showcase slide titles and stat numbers, 500 for step titles, sentence case throughout — tracked caps are reserved for labels and never used at display sizes. Sizes step down one notch because the mono sets wider: headers `clamp(28px, 3.2vw, 44px)`, slide titles `clamp(32px, 3.6vw, 52px)`, step titles `clamp(24px, 2.4vw, 34px)`, line-height 1.15. The italic-mix device retires outside the hero. Serif survives on the hero headline and the wordmark (nav + footer) only. Every `SEC. NN —` prefix is gone sitewide; eyebrows are the bare label word, and the showcase loses its eyebrow entirely. Verified: `SEC. NN` absent at 390/768/1440 and under reduced motion | — |
| 2026-08-01 | P-2's selector list was aimed at classes that do not exist | `.er-step__t` and `.er-row__t` were in the mono block; the process titles are `.er-jbeat__t` and `.er-jlist__t`, which were not — so the step titles were still rendering in the serif after the pass "landed". Caught by reading the resolved `font-family` off the live element rather than trusting the stylesheet. Now `"Geist Mono" 500 34px` at every beat | — |
| 2026-08-01 | The manifesto and the CTA (`.er-h2`) go mono too, at `clamp(28px, 4.2vw, 60px)` | P-2's "serif survives in exactly two places" is unambiguous, and these are neither. They stay the largest voice on the page, one notch down, because the mono sets wider than the serif did | Leaving them serif (would make "exactly two places" mean four) |
| 2026-08-01 | **P-3 — steps 01 and 03 get formations of their own** | Step 01 is a **datum**: the field converges out of the right into one thin ruled line left of centre, with 11 tick clusters at even intervals — "nothing is measured yet → the first reference line is drawn". Step 03 is a **column field**: a 5×3 isometric grid of dot-built columns at varied heights, points placed on the column faces only so the edges stay sharp, read as "data being compared" on arrival. Both are formation-target buffers on the same particle system, inside the existing brightness cap, and settle statically under reduced motion | — |
| 2026-08-01 | The real cause of the smeared "Analysis" step was the **beat-to-keyframe mapping**, not the geometry | Five beats spread across four keyframe gaps meant step 03 sat at story ≈2.5 — an exact 50/50 blend of two unrelated forms, which is what "noise" looked like. The spine is six keyframes now, and each beat morphs to its own form over the first 45% of its span and then **holds**, so every step is read against a settled silhouette: `01→datum · 02→site · 03→columns · 04→page · 05→page, held`. The ribbon the showcase hands over is the lead-in to 01, not a beat of its own. Still a pure function of scroll progress — no tweens, exact when scrubbed backwards | Nudging the old mapping's easing (would move the smear, not remove it) |
| 2026-08-01 | A live `ReferenceError` shipped in the middle of P-3 and was caught before commit | The `LATTICE` buffer was replaced by `DATUM`/`COLUMNS`, but the re-centring pass still named `LATTICE` — enough to throw and take the whole scene down. Fixed, and the orphaned `buildLattice` deleted rather than left as dead code | — |
| 2026-08-01 | **P-4 — the record lifts out of the faint tier** | Institution names, stat labels and the institutions-list label move to `--ink` at mono 500, 13px, label letterspacing; the three stat captions to 500 at full `--ink-dim`; stat numbers to mono 600 at the same size; hairlines up one step to `--hairline-hi`. Measured on the real surface at 390/768/1440 and under reduced motion: every string in the section **15.7:1**, except the three stat captions at **6.43:1**. Lowest value in the section is 6.43 against the 4.5 floor | — |
| 2026-08-01 | **P-5 — the clients river is rebuilt as three contained columns** | Layout only; the mechanics are the ones WI-7 established and are unchanged — one transform per column, drifts slower than reading speed, slows on hover, duplicate set `aria-hidden`, static grid under reduced motion. Three columns ≥1280px, two at 768–1279px, one below; loops of **26s / 32s / 29s**; column tops offset 0 / 96 / 48px so the cards never settle into rows; 48px between cards; quote at mono 400 16px/1.7 capped at 40ch, name in the label style | — |
| 2026-08-01 | The river's overflow mask is the **column box**, not the viewport | That was the actual repair: the old lanes were viewport-wide, so cards crossed the container edge. Measured at 1440 / 1024 / 390: cards inset **24px from both container edges at every width**, columns 418 / 464 / 342px, never touching | — |
| 2026-08-01 | The river's column count is decided in **JS, not by hiding a column in CSS** | A third column hidden at a media query takes a third of the reviews out of the page with it. `useSyncExternalStore` over the two breakpoints re-deals the list instead, so every review is present at every width | `display: none` on the third column (silently drops content) |
| 2026-08-01 | A column's loop distance is `max(set height, mask height + 48px)` | If the set is shorter than the frame, the `aria-hidden` duplicate is on screen at the same time as the original and the column reads as the same quote printed twice. Padding the loop instead means a short column shows an honest empty stretch between passes | Repeating a column's cards to fill (fabricates volume) |
| open | **TODO(content): four reviews is thin for three columns** | Still the WI-7 note, now sharper: at four reviews a column carrying one quote has a long gap in its loop. Nine or more and the columns fill and the drift reads continuously. Drop entries in and they redistribute round-robin | — |
| 2026-08-01 | **P-6 — the footer wordmark comes down to `clamp(40px, 5vw, 72px)`** | Centred, 64px above the bottom hairline, `--ink` at full opacity. Column layout and bottom bar unchanged. Measured 72px at 1440, 40px at 390 | — |
| 2026-08-01 | **Design-lock closing pass** | 390 / 768 / 1440 plus a reduced-motion run at 1440. **No page or console errors in any mode.** Hero furniture absent, `SEC. NN` absent, burger present as a `<button>` at every width. Serif confined to the hero headline and the wordmark. Journey copy and formation in sync across all five beats. River contained at all three widths and static under reduced motion. Footer wordmark 72/40px | — |
| 2026-08-01 | Lighthouse, throttled mobile: **a11y 95 · best-practices 100 · SEO 92 · performance 33** | CLS 0.024. The performance number is measured against the **dev server** — unminified bundles, no build-time optimisation — on software GL in a container, so it is not a production figure and should not be read as one. It is logged as taken rather than omitted | — |
| open | **Two Lighthouse a11y findings sit inside the regression guard** | (1) The stacked showcase's `.er-label` tags take the slide's own hue and measure **2.28–3.77:1** — the colour worlds are explicitly guarded, and the fix is a colour decision, not a polish one. (2) The trusted strip's logo `img` alt duplicates the caption beneath it (`image-redundant-alt`); the strip is guarded too. Both are surfaced rather than silently resolved, per the standing rule on LOCKED items | Fixing them inside a pass whose guard names those exact components |
| 2026-08-01 | **Ruling 1 — a second token per colour world: `--hue-NN-text`** | The pair is the lock, not just the hue. `--hue-NN` stays the world — particle tint, wash, hairlines, button borders, progress fill — and at L 38-45 it may never colour type. `--hue-NN-text` is a 45/55 mix of the same hue into `--ink` and is the only one that may: `01 #BEB19B · 02 #9CA9B5 · 03 #B69C9E · 04 #A9A1B5 · 05 #9CB1AC · 06 #B6A89B`. Declared in `studio.css` and mirrored in `StudioShowcase.jsx` as `hue` / `hueText`, because the GL tint needs a literal and cannot read a custom property. Change one, change the other | Lifting the hues themselves (would re-key six colour worlds to fix six labels) |
| 2026-08-01 | The flagged element was a **label, not a display numeral**, so the ruling's carve-out did not reach it | `.er-sc__n` is set as an `.er-label` at 11px — small text, 4.5 floor, not the 3.0 large-text rule the carve-out relies on. It measured 2.28-3.77:1 across the six worlds. Now on the text token: **7.16 / 7.61 / 7.16 / 7.35 / 8.09 / 7.88** on `--bg` and **7.52-9.08** on `--bg-deep`, at 1440, 390 and under reduced motion. World 03 lands at 7.16 on `--bg`, a hair under the 7.2 the ruling quoted, and comfortably over the floor. No hue numeral is set at display size anywhere in the build, so the exception is currently unused — it is written down for when one is | — |
| 2026-08-01 | The orphaned index-row CSS is **deleted, not left dormant** | `.er-row*`, `.er-index*` and `[data-index]` lost their markup when WI-1 replaced the index with the pinned showcase. The rules survived, and two of them coloured TEXT from a raw `--row-hue` — a second, contradictory hue-on-text system sitting in the same file as the lock. ~140 lines removed; nothing in the markup referenced any of them | Leaving them as dead code (a lock with a counter-example in the file is not locked) |
| 2026-08-01 | **Ruling 2 — the trusted-strip logos become decorative** | The visible caption owns the bank's name, so `alt` repeating it made every logo announce twice. `alt=""` on the `img`; the caption is untouched and stays in the accessibility tree. Measured: 30 images all `alt=""`, 30 captions, **15 announced names, 0 duplicates** — the duplicate set that makes the drift seamless is `aria-hidden`, so each bank is announced exactly once | `aria-hidden` on the caption (would have silenced the readable one) |
| 2026-08-01 | **Ruling 3 — the river's layout follows the volume of copy, not just the viewport** | Columns are the narrower of two limits: what the width allows (3 ≥1280, 2 ≥768, 1 below) and what the copy can fill (3 needs ≥9 reviews, 2 needs 5-8). At four or fewer there is nothing to drift, so the section stops pretending and renders as a still single column held to a 620px reading measure. With the repo's four reviews that is the state today, at every width and under reduced motion. Verified: **0 drift columns, single-column grid, 4 quotes, 620px at 1440 and 342px at 390, no page errors** | — |
| 2026-08-01 | **Ruling 4 — Lighthouse, dev vs production, throttled mobile, side by side** | Dev server: **perf 33 · a11y 95 · BP 100 · SEO 92**, FCP 4.1s, LCP 13.3s, TBT 1370ms, CLS 0.024. Production (`next build && next start`): **perf 62 · a11y 100 · BP 100 · SEO 66**, FCP 1.1s, LCP 7.4s, TBT 350ms, **CLS 0**. Accessibility reaches 100 with rulings 1 and 2 in. CLS passes the ≤0.05 gate outright at 0. SEO drops on production only because `is-crawlable` sees the deliberate `<meta name="robots" content="noindex, nofollow">` this candidate page carries — not a defect | — |
| 2026-08-01 | Performance is **62 against the ≥80 lock acceptance — it does not pass** | Profiled before touching anything, as instructed. Neither named suspect was the dominant cost. LCP is the loader's mark, and its composition is TTFB 6% · load delay 4% · load time 11% · **render delay 78% (5.8s)**. Main-thread work is 5.2s, of which **script evaluation is 2.66s** and bootup 2.8s — LCP is waiting on the main thread, not on bytes | — |
| 2026-08-01 | Four fixes came out of the profile; three were real wins that the score does not show | (1) **Wix Madefor, 150KB**, the single largest asset on every page — an unhinted TTF loaded as a *second* fallback behind Cormorant, which loads from the same layout. Removed; Cormorant plus generic serif is the chain. (2) **Geist Mono 600 was never loaded** — P-2 made the mono the display voice at weight 600 and the `@font-face` set stopped at 500, so every header, slide title and stat number was browser-synthesised bold rather than the real cut. This is a P-2 correctness defect, not a perf one, and would have shipped inside the lock. (3) Slide images are `eager` in the **pinned track only** — the stacked fallback is an ordinary vertical list with five of six below the fold, and eager there put ~300KB in front of first paint on the phone least able to afford it. (4) The loader's mask is a `url()` inside the stylesheet, discovered only after CSS parses; preloading cut LCP load delay from **26% to 4%**. Font payload on `/studio` fell ~595KB → ~445KB | — |
| open | **The gap from 62 to 80 is behind LOCKED items** | What remains is main-thread script evaluation, and the three levers that would move it are all design decisions this pass locked: not running the particle field on phones (it is visibly active at 390 today), removing or shortening the 2.9s scripted loader, and cutting Fraunces' three variable axes (146KB, for the hero headline and the wordmark). Surfaced rather than resolved, per the standing rule. If the ≥80 gate outranks any of the three, say which and it is a small change | Quietly trimming the field or the loader to make a number |
| 2026-08-01 | **Type rule, final form: the mono caps at 52px and the serif takes everything above it** | Every display role at or under 52px stays mono as built — section headers, step titles, slide titles, stat numbers. Above 52px the page speaks in the serif, and there are exactly three such instances: the hero headline, the manifesto and the wordmark. Measured at 1440 / 768 / 390: the largest mono anywhere on the page is **52.0px** (the CTA, sitting on the cap), with slide titles and stat numbers at 51.8. No mono role exceeds the cap at any width, and no role outside those three resolves to a serif family | — |
| 2026-08-01 | **The manifesto reverts to the display serif** | Back to `clamp(34px, 4.6vw, 72px)`, line-height 1.15, centred, with the roman/italic mix P-2 had retired — the pre-P-2 treatment. The scroll-scrubbed line reveals are untouched; only the face and the scale change back. The CTA, which shared `.er-h2` with it, stays mono and comes down from 60px to sit on the 52px cap | Reverting `.er-h2` wholesale (would have taken the CTA to serif and made "exactly three" mean four) |
| 2026-08-01 | **Step 01 lag: the whole position pipeline moves to the GPU** | Audit (a) confirmed on the first read. Every frame the scene rewrote `count * 3` floats into the position attribute and re-uploaded it — at 64,000 points that is **192,000 float writes and a 768KB upload per frame**, unconditionally, plus a second full `count * 3` lerp in `apply()` on every scroll update while a morph was in flight. `position` and a new `aTo` are now two of the precomputed formation buffers, bound and never written; `uMorph` is the eased blend, `uBandMix`/`uBandOffset` fold in the trusted-by band, and the wander is the same sine evaluated per vertex from static `aWPhase`/`aWRate`. `apply()` is O(1). **Zero position uploads while the page is running** | Special-casing the datum (the cost was the pipeline, not that formation) |
| 2026-08-01 | Rebinding the morph pair costs **one upload, not two** | Crossing a keyframe forward, the new "from" IS the old "to" and its buffer is already resident, so the two attributes swap roles and only the genuinely new buffer uploads. Uploading both was worth one visible dropped frame at each beat change — measured at **11.4ms**. `frustumCulled` is off on the points, or three would recompute a bounding sphere over 64,000 points every time it saw a new position attribute | — |
| 2026-08-01 | **Measured: CPU frame time through 01→02, p95 6.2ms → 0.3ms** | Every rAF callback timed on the production build at 1440 across a scripted 4s traverse of the morph. Before: p95 **6.2ms**, max **9.4ms**, mean 1.19ms. After: p95 **0.3ms**, max **0.9 / 1.0 / 2.6ms** across three runs, mean 0.10ms. Against the <4ms acceptance, every frame passes — including the keyframe crossing, which is where the remaining maximum sits. Lighthouse main-thread work fell 5.2s → 4.6s and bootup 2.8s → 2.3s; the performance score itself bounces 59-64 run to run on software GL and is not the signal here | — |
| 2026-08-01 | The datum's **ticks become brightness, not geometry** | Per the ruling: a bias baked into the formation at init as `aTick`, faded in by `uDatum` so it shows only while the datum is the form on screen. The earlier version spent 28% of the point budget on short vertical clusters — geometry doing a job a float can do. One precomputed buffer, one draw call, no second pass | — |
| 2026-08-01 | The bias had to run **down, not up** | Multiplying the ticks brighter did nothing visible: on an additive field whose bright points are already at the ceiling the boost saturates and the marks disappear. Holding the ticks at full value and dropping the run between them to 40% — with a small matching step in point size — reads as a ruled line with bright nodes, which is what a tick mark is | Raising `uCap` for the datum (would have lifted the whole field, not the ticks) |
| 2026-08-01 | Audit (c) clean | No `ScrollTrigger.refresh()` fires during a step transition — the two calls are one-per-mount (`StudioJourney` setup) and one on `window.load` (`StudioQuiet`, after images settle the layout). No `getBoundingClientRect` in any scroll or rAF path: `StudioQuiet.push()` recomputes on-screen boxes from stored page-space rects, and both `StudioTrusted` reads are in `measure`/`remeasure`, bound to mount and resize. The one `console.warn` in the scene is inside a `NODE_ENV !== "production"` guard — eliminated from the production bundle — and latched so it cannot repeat even in development | — |
| 2026-08-01 | **Fluid cursor trail (MIT, Dobryakov-derived), monochrome, hero→manifesto, handoff to repulsion at process** | The solver is kept — advection, curl, vorticity confinement, Jacobi pressure, gradient subtract — because the swirl and the dissolve ARE the effect. Everything wrapped around it in the original is gone: rainbow dye and colour cycling, bloom, sunrays, the shading toggle, idle auto-splats, the click burst, and dat.gui with every config surface. One dye colour, `#E4E4E0`, no controls. MIT notice reproduced in full at the head of `src/lib/fluidTrail.js`; credited in `docs/asset-licenses.md` | Writing a bespoke solver (the swirl is the whole point and this one is proven) |
| 2026-08-01 | The two cursor effects are **one gesture in two materials** | In the hero they coexist: the particle field parts around the pointer, the fluid leaves a wake behind it. From the process section on, the repulsion carries it alone — a wake drawn over a section that is trying to be read is decoration, and the repulsion already says the thing the wake would say. The field's repulsion stays global and untouched | — |
| 2026-08-01 | Sim 128, dye 512, DPR capped at 1.5, pressure at 16 iterations — and the dye is **one channel, not four** | With a single fixed colour the other three carry nothing, and the dye buffer is the largest and most-advected surface in the sim: `R16F` is a quarter of the bandwidth for an identical picture. Measured per fluid frame: **17.3 draw calls, 2.27M fragments**, of which the full-screen display pass is 1.30M — the solver itself is under 1M | Raising the resolutions (this is a whisper behind the type) |
| 2026-08-01 | z-index **0, not 1** — the sections would have been underneath it | Every section is `position: relative` at auto z-index, so they paint in the same layer as a z-index 0 canvas and tree order decides. The canvas is mounted directly after the particle field and before any content, which puts the wake above the dots and both of them under every word. At z-index 1 it would have sat on top of the type | — |
| 2026-08-01 | **Measured: peak opacity 12.8%, full fade 2.13s** | Sampled off composited pixels with the particle canvas hidden so its drift could not be counted as dye, fitted through timestamped captures. Peak against the 8-14% band, fade against 1.8-2.5s. Vorticity 18 — visible curl, short of a whirlpool. Splat radius 0.000375, ~0.15 of the original's default, widened up to 3.2x with pointer speed so a faster hand leaves a wider wake | — |
| 2026-08-01 | The decay is `exp(-d·dt)`, and its timestep is **real elapsed time, not the solver's** | Two bugs, one root. The solver's step has to stay clamped or the semi-Lagrangian trace leaves the grid, but sharing that clamped step tied the fade to the frame rate — a slow machine advanced less simulated time per wall second and the trail hung about in slow motion, measured at 3.6% still on screen after 3s. And the original's `1/(1 + d·dt)` is a first-order approximation that only equals the intended rate as dt goes to zero, so it decayed visibly slower on long frames. Exact form, real timestep: the same number of seconds at 30Hz, 144Hz and while dropping frames | — |
| 2026-08-01 | Splats are walked along the segment travelled, not dropped once per frame | One splat per frame beads the wake into dots whenever the pointer travels further in a frame than a splat is wide — any quick gesture at 60Hz, every gesture on a machine dropping frames. One splat per ~1% of viewport height along the path instead. The dye is a density per unit of path and is NOT divided across the steps (spaced splats do not overlap, so each owns its patch); the impulse IS divided, because the pointer moved once | — |
| 2026-08-01 | **Four hours of tuning were spent chasing a silent find-and-replace** | The injection ceiling looked broken — composited output kept climbing past what it should have allowed — through several rounds of re-tuning. It was set correctly the whole time, at four times the value every comment in the file claimed, because a `str.replace` had matched nothing and inserted a new comment beside the old constant without touching it. Measured 26.4% against a predicted 26.9% once the real value was found. Note for next time: a replace that silently no-ops is worse than one that throws, and the measurement was right long before the code was | — |
| 2026-08-01 | **Zone: page top to the end of the manifesto**, eased out over its last 30% | The canvas alpha runs 1 → 0 on a cubic ease across the manifesto's final 30%, then the rAF loop stops and the buffers are cleared so nothing is hanging there on the way back up. Measured composited trail across the zone: full before the fade, **delta 4/255 at 35% through it, nothing at 70%, nothing past the manifesto**. Scroll touches two things only — the alpha and whether the loop runs — both off measurements cached at mount and on resize, never read in the loop | Fading with CSS opacity alone (the loop would keep running behind it) |
| 2026-08-01 | Three machines get **no canvas in the DOM at all**, not an inert one | Touch and coarse pointers (a `touchmove` binding would fight the scroll it overlaps, and there is no hover for a trail to follow), reduced motion, and anything without WebGL2 or float render targets. Verified in all four: canvas absent, hero copy present, particle field unaffected, no errors. The element is server-rendered and taken back out on the first client pass, because pointer type and GL capability cannot be known on the server | Leaving a hidden canvas mounted (that is not what "not mounted" means) |
| 2026-08-01 | Lazy-mounted on the same idle callback as the particle field | `requestIdleCallback` with a 2400ms timeout, and the module is a dynamic import — the hero's type is on screen long before either canvas exists, and nothing in the hero waits on the solver | — |
| open | **The ≤2ms GPU frame time cannot be verified in this container** | The renderer here is SwiftShader, a software rasteriser — it runs the fluid at 12.6fps, so any GPU timing taken from it is meaningless for real hardware. What IS measured and portable: **17.3 draw calls and 2.27M fragments per frame**, and a CPU cost of **p95 0.3ms, max 4.9ms** with both canvases live and the pointer moving (73ms of JS across 15.2s — half a percent of wall time). The fluid is entirely GPU-bound and the display pass is 57% of it. The degradation ladder the brief specifies is left parameterised and unactivated — `simRes` and `dyeRes` are already options on `createFluidTrail`, and `navigator.deviceMemory` is the documented signal for the third step — because its trigger is observed jank, which cannot be observed here. Confirm on real hardware before enabling any rung | Enabling a degradation path against a condition nobody has seen |
| 2026-08-01 | **Service pages shipped — one template, six payloads, no particles, entrance-only motion** | `/services/[slug]`, six statically generated routes behind the showcase's EXPLORE links. One template component and one content map (`src/data/services.js`); there is no per-page layout fork anywhere. The only thing any page has that the others do not is a third image, and that is DATA — real estate carries `img3` and the block simply does not render for the other five | Six page files (they would agree on the day they were written and never again) |
| 2026-08-01 | These pages **inherit** the locked system rather than restating it | `studio.css` is imported, not copied, and the root carries `.er`: tokens, the two-voice type lock, the two motion curves, the hairline and shadow vocabulary all arrive with it. `service.css` adds one document layout and nothing else. The serif appears on exactly one string per page — the title, at `clamp(48px, 6vw, 88px)`, above the 52px mono cap and therefore inside the locked exception. Everything else is Geist Mono | — |
| 2026-08-01 | The nav and footer became **components**, which is what "identical to the studio page" has to mean | Copying the markup would have produced two things that agreed on the day they were written. `StudioChrome.jsx` now owns both, and the studio page renders the same two. The nav's hide-on-down behaviour moved out of `StudioClient` into `StudioNavScroll` — on the studio page it lived beside the particle field, which is fine there and impossible here: a service page needs the nav and must not pull three.js in to get it | Duplicating the markup |
| 2026-08-01 | **No canvas is mounted on these routes, and that is verified rather than assumed** | Measured on the production build at 1440 / 768 / 390 and under reduced motion, on two pages: **0 canvas elements and 0 network requests matching three / studioScene / fluidTrail**. The particle field and the fluid trail are not hidden here; they are absent | — |
| 2026-08-01 | The hero title rendered as an **empty gap**, from a trap the studio page had already documented | GSAP reads a CSS `translateY(115%)` off the computed matrix as a pixel `y`, not as `yPercent`, so tweening yPercent alone animates nothing and the mask stays closed. The studio page hands the armed state over with a `gsap.set({yPercent: 115, y: 0})` first and says so in a comment; the service template did not, and the title was invisible on every page. Fixed the same way. Worth noting that a documented trap in one file did not stop the same mistake in the next one | — |
| 2026-08-01 | Two layout defects found by measuring rather than by looking | Image 2 sat hard against the left gutter: `.er-svcfig { margin: 0 }` is declared after `.er-svc__wide { margin-inline: auto }` at equal specificity, so the centring lost. Only the block margins are zeroed now. And the real-estate aside was hung off the 960px image box rather than the 720px reading column — it is against the measure the eye is already following, so it reads as an aside to the text. Measured at 1440: paragraph 632px, image 2 centred 240-1200, aside 640px flush to the column's right edge at 440-1080 | — |
| 2026-08-01 | **AVIF only where AVIF actually wins** | The brief asks for AVIF-first with the supplied WebP as fallback. Six of the twenty generated variants came out BIGGER than the WebP, by up to 22% — all the heavily grained ones, because grain is noise and AVIF spends bits preserving what WebP discards. Shipping those "AVIF-first" would have cost bytes for a worse-compressed file. The losers are deleted and `avifVariants.json` records the fourteen worth offering; a file without an AVIF simply gets no AVIF source | Shipping all twenty because the brief said AVIF-first |
| 2026-08-01 | Contrast, targets and the disclosure, measured on all six | Minimum contrast below the hero band is **6.43:1** on every page (the chips at `--ink-dim`), against the 4.5 floor. Hue reaches text only through `--hue-NN-text`, which is the index numeral and the disclosure's `+`. The `tel:` and `wa.me` links are live, the disclosure is a real `<details>` and opens from the keyboard, and the hero image's alt names its subject on all six | — |
| 2026-08-01 | The hamburger's hit area is 44x44 while the icon stays 24x16 | The icon size is locked by P-1 and 16px is a third of what a finger needs. The hit area is extended with a pseudo-element rather than padding — padding would grow the button inside the nav's flex row and move the anchor the links slide out from, and the point was to change nothing visible. Hit-tested at 390: hits at 21px in all four directions, misses at 23px | Padding the button (would have moved the link anchor on both pages) |
| 2026-08-01 | Lighthouse mobile, production: **a11y 100 · best-practices 100 · SEO 100 · CLS 0 · performance 69 and 71** | Performance is under the >=85 the brief sets and I could not close it. Profiled rather than guessed: the preload works exactly as intended — LCP load delay **0%**, load time 3-5% — and **89% of LCP is render delay**. The bytes are in hand and the main thread cannot paint them. Two things were tried: moving GSAP off the critical path with a dynamic import (within noise, kept because it is the right architecture) and dropping `decoding="sync"` on the hero, which was holding the frame until a 1600x2000 photograph finished decoding on a throttled CPU. That one was worth ~8 points | — |
| open | **The remaining gap to 85 is the hero's own size and the font payload** | The hero is a full-bleed 1600x2000 photograph — 3.2 megapixels to decode on a throttled mobile CPU — and the srcset is prescribed as 800/1600, so there is no intermediate width to fall back to. `uses-responsive-images` reports 116-278 KiB of waste for exactly this reason. Alongside it, 264KB of woff2 loads on a page whose serif sets ONE string. A 1200w hero derivative would cut the decode by roughly 44% and is the single highest-value change available; it needs a decision on the prescribed srcset. Note also that this container scores low across the board — the same suite gives `/studio` 59-64 — so treat these as a floor, not a verdict | Generating a width the brief did not authorise |
| 2026-08-01 | **THIRD VOICE — the type lock is restated: display serif speaks · text serif reads · mono measures** | Newsreader joins as the reading face (Google Fonts, SIL Open Font License), self-hosted by next/font, latin subset, roman and italic at 400 and nothing else. Its roles are service-page paragraphs and future multi-line reading text; the About page will use it. `18.5px / 1.75 / 62ch / --ink`, hanging punctuation off. Mono keeps the chips, keep-ready, practical line, CTA, index numerals, nav and footer. The display serif is untouched. **46KB combined**, against the ~90KB the amendment allows | Source Serif 4, the approved alternate — held in reserve if Newsreader fails visual review |
| 2026-08-01 | Why a third voice at all, in one line | Mono is a poor face at paragraph length: uniform advance width defeats the word-shape recognition that makes prose skimmable. Two voices were right when the longest text on the page was a caption; these pages are documents | — |
| 2026-08-01 | Newsreader ships with **no `axes`**, which is not what the amendment asked for and is the only thing it could be | next/font rejects an axis list alongside a pinned weight — naming axes means taking the whole variable font and letting weight vary with it. The amendment asks for the opsz axis AND for 400 + 400 italic only; those two cannot both hold. The static cuts are the literal reading of the second and the smaller download, and Newsreader's static instances are already drawn at a text optical size, which is what pinning opsz was for | Loading the variable font (the full weight range, for two weights) |
| 2026-08-01 | **Two images per page, uniformly** | The `img3` block is out of the template and out of the real-estate payload; `page-01-pattern` stays in `/public` and in the licence table for future use, with no preload and no AVIF manifest entry. There is now no per-page data fork either — every page is hero plus one image. Verified: **1 figure on all six** | — |
| 2026-08-01 | **The reading fill** — scrubbed, opacity only, and the split is server-rendered | Each paragraph is split into word spans by `ReadingText` on the server, so nothing reflows on hydration and a no-JS visitor reads a finished paragraph. One ScrollTrigger per paragraph, scrubbed from the paragraph's top at 85% of the viewport to 45%, linear stagger, opacity only — no transform, so it never triggers layout. Measured left-to-right progression: first word full while the middle is mid-fill and the last still resting, and everything at 1.0 by 45%. Selection returns all 515 characters with spaces intact | — |
| 2026-08-01 | The accessible split: **no ARIA at all**, after the first attempt failed the audit | The first version hid the spans with `aria-hidden` and named the paragraph with `aria-label` — the pattern the amendment describes. axe flagged it as `aria-prohibited-attr`, correctly: a `<p>` has no role that supports an accessible name, so the label was invalid, and on readers that honoured it, it was the only thing between the user and the text. Plain inline spans need none of it: no role, no display change, whitespace INSIDE each span rather than in CSS, so the accessibility tree sees what a `<p>` containing an `<em>` would. Accessibility back to **100** | The sr-only duplicate the amendment also offers — it would have made every paragraph select and copy twice |
| 2026-08-01 | The resting tone is **0.50, not the 0.18 the amendment specifies** — two instructions in the brief collided | `--ink` on `--bg` at 0.18 opacity measures **1.66:1**. The floor this project has held on every string throughout is 4.5:1, and un-filled words are text a reader can already see, so the floor applies to them. **0.49 is the lowest value that clears it**; 0.50 is the round number above, at **4.71:1**. The effect keeps its character — half-tone to full ink is still an unmistakable fill — and no word sits below the floor while it waits its turn. Flagged rather than silently resolved, per the standing rule on conflicts | Shipping 0.18 (would have put most of the page's prose at 1.66:1) |
| 2026-08-01 | Reduced motion **unwraps the split**, rather than leaving it inert | The spans are server-rendered because the server cannot know the preference. With no fill to run there is nothing for them to do, so they are collapsed back to a single text node — verified: **0 spans, 0 ARIA attributes, 515 characters intact, nothing hidden**. "No spans" now means no spans | Leaving them at full opacity (inert is not absent) |
| 2026-08-01 | Adding a face made every page heavier, so **Newsreader is not preloaded** | Every font declared in the root layout is preloaded on every route: `/studio` was already downloading ten files and rendering with three, and a reading face it never uses would have made that 491KB. Without the preload link the `@font-face` remains and the browser fetches the file only when something actually sets a glyph in it. `/studio` is back to **8 files / 445KB**, exactly where it was; service pages pay **22KB** — the roman only, since no italic renders in these paragraphs. The reading text is below the hero fold, so the later start costs nothing visible | — |
| open | Cormorant, Archivo and IBM Plex Mono are still preloaded on routes that never use them | The same finding, one scope wider: `/studio` and the service pages render in three families and download eight files. Fixing it properly means auditing which routes use which face across the whole site and moving the page-specific ones behind `preload: false`, which is a sitewide change and out of scope for this amendment | — |
| 2026-08-01 | **Amendment floors re-run: contrast 6.43:1 minimum on all six, at 390 / 768 / 1440 and under reduced motion** | The minimum is the mono chips at `--ink-dim`; the new reading face sits at `--ink`, 15.7:1. One figure and two paragraphs per page everywhere, the fill completing to 1.0, zero page or console errors in every mode. Lighthouse mobile on the production build after the amendment: **a11y 100 · best-practices 100 · SEO 100 · CLS 0**, performance 71 and 57 | — |
| open | Lighthouse `font-size` fails on IBC and passes on real estate, from the **locked 11px label scale** | The audit wants 60% of text at 12px or more; IBC lands at **59.51%**. It is not a defect in the page — it is the locked label size meeting a coverage heuristic, and IBC trips it only because its prose is the shortest of the six, so the labels are a larger share of the total. Surfaced rather than resolved: changing the label scale is a locked typographic decision, and doing it to move a heuristic over a threshold would be the wrong reason | Raising `.er-label` off its locked 11px |
| 2026-08-01 | **/services register shipped; legacy page deleted; retired row colour-world system re-homed** | The catalogue hub, built on StudioChrome and the locked tokens. Compact hero (serif title, the one string), Newsreader intro with the reading fill, the cities satellite, the six practices as index rows, the remaining nine as a two-column hairline list, one proof row, footer. No canvas — **0 canvas elements and 0 requests matching three / studioScene / fluidTrail** at 1440 / 768 / 390 and under reduced motion | Restyling the legacy page |
| 2026-08-01 | The legacy page is **deleted, not restyled** | Gone: the stock imagery, the pastel icon circles and all seventeen lucide icons, the "Why Choose Us?" block and its six adjective cards, the legacy nav and footer, and the "Delivering accurate and independent..." copy. `Servicehero.jsx` and `WhyChooseUs.jsx` were orphaned by the deletion and removed with it. One thing survived because it was the only specific, verifiable content on the page: the nine towns the firm travels to | — |
| 2026-08-01 | **The index-row colour worlds come back**, with two changes the locks forced | The system was retired when WI-1 replaced the studio index with the pinned showcase, and its rules were deleted in the design-lock pass. Revived here with the panel at 8%, hairlines toward the hue, tinted numeral and the tag giving way to the description — unchanged. What changed: the title is **mono, not the display serif** it used to be, because P-2 put the display voice in the mono under 52px and left the serif three instances, none of which is a list row; and the italic-on-hover swap goes with it, because P-2 retired the italic-mix device outside the hero | Reviving it verbatim (would have re-broken the type lock the same pass established) |
| 2026-08-01 | Scroll-soft activation, and keyboard focus counts as a pointer | A pointer says which row you mean; a scroll says only roughly. So a pointer gets the full treatment and a scroll gets colour only, at half strength, with the description left alone — a description sliding in under a finger that is only passing through is noise. Keyboard focus gets the full treatment, which is also what makes the swap operable without a mouse. Measured: soft = panel 4% / description 0, hover = panel 8% / description 1 / tag 0, focus identical to hover, each row in its own hue | — |
| 2026-08-01 | The reading styles moved **out of a page's stylesheet and into the component** | `.er-svc__read` lived in `service.css`, and the moment the register reused `<ReadingText>` its intro silently rendered in the mono, because the register does not load that file. Caught by reading the resolved family off the live element rather than trusting the markup. Now in `reading.css`, imported by the component, so anything using it gets the face | — |
| 2026-08-01 | **CLS 0.127 on the register, traced to the font and fixed by scoping it to a segment** | Newsreader was declared in the root layout with `preload: false` — which kept it off `/studio`, and cost the routes that DO use it a font-swap reflow that pushed the rows section down. Lighthouse named the file. Declaring it in `src/app/services/layout.js` instead is what both earlier answers were approximating: the routes under `/services` preload it and never shift, every other route never hears of it. **CLS back to 0** on the register and the service pages, and the service pages' Speed Index improved from 6.1s to 2.4s with it | `preload: true` in the root (would have taxed every route to fix two) |
| 2026-08-01 | The hero title and eyebrow reveals moved from **GSAP to CSS**, scoped to the services family | Both were armed by the stylesheet and released by JS, so neither could appear until the motion module had loaded and run — an entrance that gates the LCP element on a script. The eyebrow was worse than late: it had no armed state at all, so GSAP was hiding an element the browser had already painted and then revealing it again, which is a visible flash. Anything in the first viewport now animates from the stylesheet, and the scoping (`:is(.er-svc, .er-reg)`) keeps it away from the studio page's own headline | — |
| 2026-08-01 | GSAP goes back to a **static import**, and the earlier claim that splitting it helped was noise | Measured both ways on the production build: 78 / 76 either way, identical LCP. The dynamic import was kept once on the reasoning that an animation library should not delay the paint it decorates; the numbers never supported it. Static is fewer moving parts | — |
| 2026-08-01 | **Floors: contrast 5.39:1 minimum with every row forced into its active hue world**, at 390 / 768 / 1440 and under reduced motion | 85 strings measured against their composited backdrop, including the hue panels at 8%; **nothing below 4.5**. The minimum is the proof row's arrow at `--ink-faint`. Hue reaches text only through `--hue-NN-text`. Rows are keyboard-operable and the description swap fires on focus | — |
| 2026-08-01 | Lighthouse mobile, production: **a11y 100 · best-practices 100 · SEO 100 · CLS 0**, performance 78 (register) and 75 (service page) | Under the >=85 the brief sets. FCP 1.1s and **Speed Index 2.4s** — the pages are visually complete in well under three seconds — while LCP reads 5.8s and 6.6s. On the register LCP is attributed to an 11px mono label that is neither the largest nor the last thing to paint, which does not survive scrutiny as a real user-facing delay; the two things I could act on (the font-swap CLS and the eyebrow flash) are both fixed. What remains would mean changing the locked type or the entrance design. Note also that this container reads low across the board — `/studio` scores 59-64 on the same suite | Chasing the number by unpicking locked decisions |
| open | **`/career` and `/privacy-policy` are orphaned by this page** | The legacy page reached them through the site-wide Navbar and Footer, which the register no longer renders. `/career` exists as a route and is still in `sitemap.js`, so it is reachable by URL but linked from nowhere in the new chrome. `/privacy-policy` is worse: the legacy footer links to it and **there is no such route on disk** — it has been a 404 the whole time. Both need a decision: add them to the StudioChrome nav or footer, or retire them. Flagged rather than decided, because either answer is a content call | Quietly adding them to a nav the client has not seen |
| 2026-08-01 | **REGISTER v2 — R-1: the hero splits into two columns and the right-hand void closes** | The title held the left of a 1440 container and the right half was simply air. Left column 7 of 12 (eyebrow + serif title), right column 5, vertically centred against the title block: the Newsreader intro with the reading fill, the nine cities in mono beneath it. Measured at 1440 the two tracks are **770px / 550px**, at 2560 **760.7px / 543.3px**, and at 390 it is a single stacked column. Hero height is content-driven against a `64svh` ceiling and lands at **352 / 407 / 540px** — the cap is never the thing setting the height | Padding the title block out to fill the width (the void would have become a wider void) |
| 2026-08-01 | **R-2: the row spec enforced — 40px hard cap, 44px padding, three fixed tracks** | Title mono 600 at `clamp(26px, 2.4vw, 40px)`, measured **40px at 2560 / 34.56px at 1440 / 26px at 390** — the 52px ceiling belongs to section headers and a row title sits under one. Grid `[64px][1fr][280px]`, `padding-block: 44px`, slot measured at 280px. Six rows now stand at **140px each — 841px, 0.93 viewports at 1440**, against the four-viewport sprawl the repair was called for | Leaving the 52px cap in place for rows |
| 2026-08-01 | **`Explore →` came out of the grid flow — it was charging every row 43px for a line nobody could see** | In flow it created an implicit second grid row that all six rows paid for in height whether the affordance was visible or not. It is an invitation to a link the whole row already is, so it should cost nothing until it appears: absolutely positioned against the title column's left edge. Rows went **183px → 140px** on that change alone | Reserving the space (the row is a link with or without the words) |
| 2026-08-01 | **The tag labels were laid out as flex items, and three of them in a 280px slot overlapped each other** — found in the 2560 screenshot, not by the check that was supposed to catch it | Each label was its own box inside a flex container, so each was sized from its own content, each shrank past it when the three met the slot, and each wrapped its own separator onto a second line — which is why the middots floated above the words. Measured: three items of 117 + 162 + 117px inside a 280px box, overflowing the slot to the left. Now **one inline run** in a single block, with `nowrap` on each label and a no-break space gluing each separator to the label before it, so a line can only break AFTER a middot. Re-measured at 2560 / 1440 / 390 and under reduced motion: **0 labels overflowing the slot, 0 overlapping, 0 lines opening on a separator** (the last scanned per character via Range rects, not by eye). The same glue fixed the cities line, which was opening its last line on a stray `·` at 390 | The tracking or the 11px label scale (both locked, and neither was the fault) |
| open | **Row 01's tags run to three lines, against R-2's two** — surfaced rather than resolved by deleting a service | `Bank mortgage · Court & settlement · Capital gains` is ~430px of tracked 11px mono; the 280px slot and the `nowrap` labels leave line 1 holding only the first. Three constraints meet here and one has to give: the 280px slot (R-2), the two-line cap (R-2), and the locked 11px / 0.18–0.25em label scale (§4, and the low end of the range still misses by ~1px). The fourth option is dropping the third label, which is real business content and the standing rule says not to. **It costs nothing in layout** — the slot reserves 3.2em (52.8px), three lines occupy 49.5px, and all six rows measure a flat 140px. If the two-line cap outranks the third label, it is `slice(0, 3)` → `slice(0, 2)` in `page.js` and nothing else | Silently trimming a service off the row to make a line count |
| 2026-08-01 | **R-3: the proof counter is gone.** `02 OFFICES · 09 CITIES · 15 INSTITUTIONS` replaced by one sentence | A register is not a scoreboard. One mono sentence in sentence case — "IBBI and Income Tax Dept. registered. Reports accepted by banks, tribunals and departments across Madhya Pradesh and Maharashtra." — linking to `/studio#er-record`, where those figures already live and are set out properly. Verified absent: **0 elements matching the counter format** at every width | Keeping the counters and restyling them |
| 2026-08-01 | **R-4: brand mark restored to chrome.** The roundel at 26px in the nav and 44px in the footer | Mark and wordmark are **one link** in both places, not two adjacent links to the same destination — a nuisance to anyone tabbing — and the mark carries `alt=""` because the wordmark beside it already says the name. No SVG of the roundel existed, and it was not traced: the source `SSAdhauBG.png` has a fully transparent background, so its **alpha channel already is the silhouette** — cropped to the roundel, taken as a mask and painted white (`public/images/mark-white.png`, 160×162, 8KB). Exact by construction rather than approximated by a trace. Present in nav and footer on `/studio`, `/services` and a service page at 2560 / 1440 / 390 | Autotracing the raster (an approximation of a shape we already had exactly) |
| 2026-08-01 | **R-5: the footer's dead vertical span is gone, and it was one stacked margin, not the layout** | The column block now anchors to the bottom: `96px` from the columns to the bottom-bar hairline, `128px` maximum above them. The giant centred `.er-bigmark` wordmark is deleted and replaced by the roundel plus a small serif wordmark, left-aligned above the columns. The actual dead span was `.er-colophon`'s own `clamp(24px, 4vh, 48px)` top margin sitting on top of the 96px — removed. Measured on `/studio`, `/services` and a service page at 2560 / 1440 / 390: **content-to-hairline exactly 96px in all nine** | Adding a fixed footer height (would have papered over the stacked margin) |
| 2026-08-01 | **R-6: the two 'also within scope' columns start level, and the closing line shares a baseline with the button** | `grid-auto-rows: min-content` stops a tall item in one column stretching its neighbour, which is what made the tops drift apart; items are spaced 40px, split across the hairline. The closing line and SPEAK TO A VALUER sit on one row at `align-items: last baseline` — the sentence rests on the button's text rather than floating beside its box | Centre-aligning the pair (the line would float against the button's padding) |
| 2026-08-01 | **R-7: nothing but the page background exceeds 1440 at 2560** | Audited every painted box on the register at 2560 — background, border or image, visible, taller than 1px: exactly one hit, `DIV.er.er-reg` at 2560, which is the page ground and is meant to be full-bleed. Content measures 1391px inside the 1440 container. At 1440 and 390 the list is **empty**. Reduced motion is clean on the same checks with **0 console errors** at all four settings. Elsewhere the audit finds the specified full-bleed bands only — the service hero and its veil, `/studio`'s scrim | — |
| 2026-08-01 | **A check can pass while the page is visibly broken** — the tag defect is the case in point | The verification script asked whether any single label split across lines. It could not: `white-space: nowrap` guarantees it. So the check reported clean on four viewports while the labels were overlapping one another and the separators were stranded above the text — and the 2560 screenshot showed it at a glance. The rule the earlier `DYE_CEIL` incident produced holds here in a new form: **the assertion has to be able to fail for the reason you are actually worried about.** The checks now measure overflow past the slot, overlap between labels, and per-character line-opening — all three of which can fail | Trusting a green check over a screenshot |
| 2026-08-01 | **Scope ledger rebuilt: reading text to Newsreader, paired-rule grid, hand-drawn glyph set, scrubbed rule drawing** | The section was nine titles and nine descriptions all set in `.er-label` — tracked uppercase from top to bottom, in a lock that reserves tracked caps for labels of four words or fewer. Titles are now mono 500 at 19px, sentence case, normal tracking, `--ink`; descriptions are Newsreader 400 at 16.5px/1.65 in `--ink-dim` at 40ch, the same reading face the service pages use. Audited across the whole section at 1440 / 390 / reduced motion: **exactly two things still carry tracked caps — the eyebrow and the CTA** | Restyling the labels (the problem was the voice, not the size) |
| 2026-08-01 | **R-9 — one grid owns both columns, so the ledger cannot drift** | The old section was two independent stacks of bordered list items, and two stacks drift: one description wrapping to a third line put every rule below it out of step with its neighbour. Now `1fr 96px 1fr` with the rule as a full-width row of the same grid. Measured at 1440: the rule's gap is **96px and its two edges land exactly on the two columns' inner edges (0px error on both)**, and the four pairs' tops are level to **0px**. The ninth item keeps the left column with the rule still running the full width | `grid-auto-rows: min-content` on two stacks (v2's fix — it levelled the tops but not the rules) |
| 2026-08-01 | **R-11 — the rules are ruled, not faded.** Each is two halves growing away from the gutter, scrubbed | One ScrollTrigger per rule, transform only, scrubbed between the rule's entry at 92% of the viewport and 65% — so the scroll is what draws the line, which a fired tween could not be: it would run at its own speed however the reader moved. Sampled at five points through the band and the halves read **0 / 0.247 / 0.498 / 0.749 / 1.000**, monotonic, both sides in step. The head rule above the eyebrow is the one rule with no gap, because a gap belongs to a pair of columns and that rule heads the whole section | Fading the rules in (the ask was explicitly that scrolling rule the ledger) |
| 2026-08-01 | **R-10 — nine glyphs, hand-written here.** lucide stays deleted and does not return through a side door | 24×24, single stroke at 1.25, no fill, no circle, no colour of their own — hairlines, which is the only line language this site has. Every path carries `pathLength="1"`, so one dash array is right for all thirty paths whatever their real arc length: without it the umbrella would finish drawing while the pylon was still halfway up. Verified: **30 paths undrawn above the fold, 0 undrawn after the rows enter**, hover runs `er-glyph-draw` once at 0.4s and returns to 0 | An icon library (the legacy page's seventeen lucide icons were deleted for this reason) |
| 2026-08-01 | **Four of the nine glyphs failed visual review on the first cut and were redrawn** — two of them read as the letter A | Rendered at 4x and judged side by side rather than in place, which is the only way to see a 24px drawing. The pylon's crossarms were flush with its legs, so it read as a capital A with a brace; they overhang now. The compasses had a bar between the legs — a second A, next to a triangle — and the hinge is a cap across the apex instead. The balance's pans were too shallow to be pans. The seal's inner bar read as a minus sign and is gone, which also matches the brief (it asked for an outline and ribbons, and specified no inner mark) | Shipping the first cut on the strength of the code being correct |
| 2026-08-01 | **`stroke-dashoffset` is a deliberate exception to the transform/opacity-only rule in §2** — surfaced, not silently taken | The performance lock says animate `transform` and `opacity` only, and R-10 asks for drawing, which no transform can express. Taken because the cost is bounded and measurable: 30 paths inside nine 24×24 boxes, paint-only with no layout, running once per row on entry and once per hover — not a scrubbed property and not a per-frame attribute upload. The rules beside them, which DO run on scroll, are transform-only as the lock requires | Faking the draw with a clip or a mask (more machinery, same paint, worse drawing) |
| 2026-08-01 | **R-12 — the closing line is a sentence, so it is set as one.** Newsreader italic at 22px, `--ink` | It was an `.er-label`: tracked caps, `--ink-dim`. The dead space under the button was two paddings meeting — this block's bottom and the proof block's top, **~162px of nothing at 1440**. One of them sets the clearance now: measured **96px from the button to the proof rule and 96px from the proof line to the footer**, at 1440, 390 and under reduced motion. Note this settles a conflict inside the brief: R-8 keeps the closing LINE in tracked caps and R-12 moves it to the reading face. R-12 is the more specific instruction and wins; the tracked caps that remain are the eyebrow and the button | Keeping the line in the label voice (R-8's letter, against R-12's reason for existing) |
| 2026-08-01 | **R-10's "the title lifts to full `--ink`" has nowhere to lift from, because R-8 sets the title at `--ink` already** — RULED, closed | Two instructions in the same brief: R-8 fixes the resting title at `--ink`, R-10 describes hover as lifting it there. Resolved in R-8's favour — it is the explicit type spec — and the hover response moved to the glyph, which goes `--ink-dim` → `--ink` as it redraws. So the row still answers the pointer, and the answer is the drawing rather than the text. **Ruled 2026-08-01: correct, and do NOT dim the resting titles** — this section's whole problem was dimness and noise, and full `--ink` at rest is the readability call. R-10's title-lift language is dead; the glyph redraw carries the hover feedback alone | Dimming the resting title to manufacture somewhere to lift from |
| 2026-08-01 | **Two mobile defects the desktop build hid**, both found in the 390 screenshot | (1) The rule belongs to a PAIR, and stacked, a pair's two items are vertical neighbours — so the ledger was ruled after every second item and the first ran into the second unruled. There is now a between-items rule that is `display: none` above 820 and never gets a ScrollTrigger while it has no rects. Measured: **9 items, 0 unruled, at 1440 and at 390** (6 visible rules against 10). (2) The CTA broke as "SPEAK TO A / VALUER · +91 8793 000 / 929"; its two halves are unbreakable boxes now, so the line can only break between them — the same fix the row tags needed, and the same defect | — |
| 2026-08-01 | **PRECEDENT — a sentence is reading text wherever it sits.** The tracked-caps rule is amended in §4 | Ruled on the register's closing line, which R-8 kept as a label and R-12 moved to Newsreader italic. R-12 wins, and the general rule moves with it: **tracked uppercase is the label voice — four words or fewer, naming a thing — and any full sentence is reading text even in a closing position.** Position does not make a sentence a label. This is the rule to apply next time the question comes up rather than a decision about one line: straplines, sign-offs and closing lines are all sentences, and all take the reading face | Reading the tracked-caps rule as being about placement rather than about what the words are |
| 2026-08-01 | **RULED — resting titles stay at full `--ink`, and the hover response lives on the glyph** | The scope ledger's failure mode was dimness and noise; dimming the titles to give a hover somewhere to travel would have re-created the problem the section was rebuilt to fix. So the resting title is `--ink` and stays there, the glyph carries the whole hover response — `--ink-dim` → `--ink` as it redraws once at 0.4s — and R-10's title-lift language is retired. Shipped as built; nothing to change | Dimming nine titles to animate a colour |
| 2026-08-01 | **CODIFIED — the motion lock's property test is per-frame cost, not the property's name** | §2.3 read "animate `transform`/`opacity` only", which is a rule about frame budget written as a ban on a property, and the scope ledger's drawing glyphs had to be taken as an exception to it. It now reads: **`transform`/`opacity` only for anything driven per-frame by scroll; bounded paint properties (stroke-dash on small SVGs, ≤ once per trigger) permitted for entrance and hover.** The split the ledger already ships is exactly the intent — the rules scale on scroll and never redraw, the glyphs redraw once on entry and once per hover and never scrub. Recorded in §2.3 and §7 | Leaving the lock as written and carrying a standing exception against it |
